# Jomon — Authoritative Development Plan

## Authority and maintenance

This file is the source of truth for Jomon’s delivery process: product constraints, work order, task status, acceptance criteria, and open decisions. `LORE.md` is the canonical setting and content reference. If the documents conflict, do not implement the disputed behaviour; resolve and record the product decision in both documents first.

Status markers have a strict meaning:

- `[x]` complete: implemented where applicable and verified against its stated acceptance criteria.
- `[-]` active: the single current implementation slice.
- `[ ]` planned: not yet started. Do not infer implementation from a planned item.
- `Decision`: needs explicit product-owner direction; do not fill it with an unrecorded assumption.

At the end of every completed slice, update this document: mark only verified work complete, add the verification actually run, split the next item when it becomes clearer, and record any decision that changes scope or direction. Do not recreate superseded plans or treat Git history as active direction.

## Current state

Jomon is being rebuilt as an original low-mysticism late-medieval river-and-coast roguelike. The checked-in game is a substantial but superseded space-fiction prototype. It may inform architecture and testing technique, but its user-facing lore, vocabulary, assets, progression, save migrations, routes, and content are not requirements for the medieval game.

The documentation reset and Phase 1.1 are complete. The rebuild now has a deliberately minimal, non-gameplay bootstrap: a deterministic foundation household, an existing zero-time initial-courier runtime seam, isolated local records, and an original keyboard canvas shell; its authored Chromium keyboard flow also passes. The generation-configuration, manifest-provenance, content-boundary, initial-world-pipeline, expanding-frontier, full-colour semantic palette, settings UI, generation-test, action-clock, mutable-world-state, persistent-person, fidelity-planning, due-event catch-up, simulation-outcome safety/provenance, corrective catch-up-integrity, world-era, causal-history, delegation, bounded autonomy, management-sidebar, social-memory/evidence, delegation/autonomy hardening, terminal-presentation, original ASCII glyph-vocabulary, keyboard-first interaction, detailed-renderer adapter, renderer/browser compatibility-test, desktop-browser performance/storage-budget, compact indexed-local-persistence, profile-guided optimization-boundaries, deterministic performance-fixture, closed internal-content contracts, renderer-independent effect-model, mystical-effect policy/audit, courier-loss policy/assessment, Jomon-integrity policy/assessment, deterministic effect/loss/recovery cross-contract tests, and the compact ASCII deck-plan are complete. The initial-household contract remains the sole active Phase 2.2 task; no tavern switching, gangplank/quay departure, trade, combat, travel, detailed renderer, or domain simulation gameplay is represented as complete.

## Context-free implementation handoff

Any coding agent begins by reading `LORE.md`, this file, and `README.md`, in that order; then checks `git status` and preserves unrelated work. These three root documents define the medieval game without requiring chat history. This repository’s existing TypeScript application is a non-canon space-fiction prototype. It is useful only for narrow technical reference—especially its terminal canvas, test conventions, local persistence error handling, and the spatial idea of a walkable vessel with a sidebar.

- Begin all new medieval domain code under `src/medieval/`, with small, named modules and colocated focused tests. The medieval domain must not import prototype gameplay, save schemas, lore, content, or progression types.
- Do not rename, migrate, or reinterpret prototype modules into medieval systems. Keep the prototype isolated until a completed roadmap item specifically replaces a user-facing path; preserve unrelated prototype tests unless their removal is itself a verified task.
- The medieval app receives its own root state, renderer-independent domain contracts, IndexedDB namespace, and browser bootstrap path. Its only temporary shared dependencies may be generic browser/tooling primitives that carry no prototype game meaning.
- Treat a seed plus resolved `WorldGenerationConfig` plus generator version as the reproducibility boundary. Do not add per-person rerolls, unseeded randomness, wall-clock simulation, network authority, or hidden state mutations.
- Add or update a focused test before each new behaviour. A passing prototype test suite is not evidence that a medieval feature exists. Keep exactly one `[-]` roadmap item active and update this file only after verification.

## Non-negotiable product decisions

- Jomon is a persistent itinerant household on a river-basin-to-coast network.
- Every new world starts with a wholly generated Jomon household. Its roster, identities, roles, relationships, histories, equipment, and active-courier eligibility derive deterministically from that world’s seed and resolved configuration, so the same manifest recreates the same crew and world.
- New-world creation shows the generated eligible crew and lets the player choose one initial courier. There are no individual roster rerolls: changing the crew means changing the seed or generation settings. Later voluntary perspective changes happen physically at Jomon’s tavern.
- The player inhabits rotating Jomon crew members. A truly lost courier is permanent, subject only to exceptionally rare and explicit mystical safeguards. If no living eligible crew member remains, active play ends and the world becomes a read-only, exportable chronicle even when Jomon still physically exists.
- Jomon’s tavern, chart table, cargo hold, repair space, stores, berths, galley, and gangplank are walkable map spaces. Physical props use compact contextual key-choice prompts. A deliberately comprehensive management sidebar is visible by default, compact, and collapsible; it shows all facts known by the household—each with source and freshness where relevant—but never undiscovered global truth. It complements physical surfaces without replacing their direct action or situated information, and the map remains primary.
- Gangplanks and quays replace airlocks and landing terminals.
- Trade uses physical commodities with capacity, condition, handling, loss, recovery, local supply, demand, and market consequences.
- Human conflict, wilderness, and operational hazards must receive equal systemic depth.
- Combat remains turn-based and grid-based, but its old content and progression model will be redesigned.
- Jomon is low-mysticism medieval fantasy. Rare relics, totems, boons, curses, rites, omens, and other uncanny forces can have real mechanical effects, but there is no generic mage class or unlimited free-form spellcasting.
- Simulation time is action-driven, not wall-clock-driven. It advances only when the player takes or confirms a time-bearing in-world action—such as movement, waiting, travel, work, rest, or delegated-task commitment/resolution. Pure inspection, sidebar use, and an idle open tab never advance it. All consequential randomness is seeded and inspectable.
- Jomon is an open-ended persistent world simulation, not a finite campaign to be won. Short-term goals provide direction; exploration, relationships, and the changing household/world provide the long-term play.
- The player is not bound to a single protagonist. On an active courier’s death, control transfers to another eligible crew member; voluntary perspective changes occur at the tavern.
- An active courier directly controls only themself. They can delegate work to crew and NPCs through conversation; delegation depends on the courier’s `conversation` stat and the other person’s relationship, role, capacity, interests, and current situation.
- Failure is causal and diegetic. A courier death, lost cargo, unpaid debt, damaged route, broken relationship, or ruined vessel can permanently change the world. The irreversible collapse or loss of Jomon ends active play in that world and converts it to a read-only, exportable chronicle.
- Jomon can grow through new rooms, refitted workspaces, tools, crew capacity, and small craft. Every expansion is physical, persistent, grounded in available materials/labour, and balanced by upkeep, space, staffing, cargo, route, or social trade-offs.
- A continuing world has escalating difficulty eras: base, NG+, and NG++. Active-play progression in the same persistent world moves it toward later eras without a reset or finite campaign ending. Each era adds durable, diegetic pressures and new possibilities rather than merely increasing enemy numbers.
- The initial release target is a desktop browser game for players who enjoy ASCII graphics and keyboard-first control.
- Jomon is single-player and offline-first. It requires no account, server, cloud sync, network connection, remote telemetry, or multiplayer authority. A local browser may retain multiple active worlds and read-only chronicles, subject to quota; only one world may be open and mutate in a browser session at a time.
- Every instantiated person has a full individual persistent record—including family, work, needs, relationships, injury, possessions, birth, and death—while distant people advance through deterministic summary simulation at an appropriate fidelity level.
- The world has an expanding procedural frontier. Newly explored regions are generated causally and become persistently explorable as Jomon travels; rumours, charts, travellers, and trade can establish their existence before arrival. An ungenerated frontier has deterministic regional commitments rather than latent mutable people; once a region or named person must exist, it is instantiated with its full persistent record and cannot later contradict prior knowledge.
- No user mod/content-pack compatibility is planned. Internal content remains data-driven and documented for maintainability, but does not promise a public extension API.
- Do not include or procedurally generate sexual violence, slavery, torture, or harm/endangerment of children. This applies to player actions, events, histories, simulation summaries, enemy behaviour, contracts, hazards, rumours, and player-facing text.
- The reset is a clean persistence break. Do not migrate the superseded space-era saves or reinterpret them as medieval campaigns.
- Do not copy lore, text, names, assets, or exact mechanics from other games. Historical and game references are influence constraints only.

## Rogue-inspired visual and interaction direction

Jomon takes clear, original inspiration from **Rogue (1980)**: a terminal-first, keyboard-led, turn-based grid game whose map is the primary visual explanation of play. This is an aesthetic and interaction constraint, not permission to copy Rogue’s copyrighted presentation or fantasy content.

- ASCII is a first-class, release-quality presentation—not a temporary fallback. The detailed renderer must expose exactly the same consequential state.
- Favor a sparse character-cell grid, limited purposeful colour, strong contrast, and immediate silhouettes over decorative pixels, painterly scenes, or interface clutter.
- The map communicates position, architecture, actors, cargo, props, hazards, routes, and discoveries directly. Every symbol must have a stable, original, documented meaning.
- Use a compact, persistent status readout and terse message feedback. The player should normally understand the current danger and affordances without opening a separate screen.
- Keep direct keyboard control and compact contextual prompts. Support familiar eight-direction movement where it improves play, alongside discoverable/remappable controls and accessible alternatives.
- Use original glyph assignments, layout, copy, assets, sounds, names, and mechanics. Jomon uses an original, semantic, full-colour palette; it may take only high-level tonal inspiration from terminal themes, never their exact palette, graphics, box art, interface layout, source code, or fiction. Colour is never the sole consequential signal.
- The medieval material world is never weakened to imitate Rogue: Jomon’s readable symbols must describe vessels, work, people, weather, cargo, tools, grounded danger, and the rare uncanny phenomena that materially affect them—not default spellcasting or generic fantasy monsters.

## Emergent world and people direction

Jomon’s world should feel alive in the sense of Dwarf Fortress Adventure Mode, Caves of Qud, RimWorld, Rogue, and Risk of Rain: it has a remembered past, people pursue their own material and mystical goals, the player can build tactical power through legible combinations, and the player’s actions become part of later situations. The player experiences the world through a courier on Jomon, supported by both situated in-world surfaces and a comprehensive management sidebar that is visible by default while keeping the map primary. The sidebar provides complete household-known information, not supernatural knowledge of hidden world state.

- The world evolves only through action-driven in-world time. Each time-bearing action processes due deterministic events across all fidelity tiers, so distant people and institutions can advance while the courier acts elsewhere. Inspection, an idle browser tab, pause, and a closed browser do not advance time.
- A world begins with a causally generated history: geography and waterways first; then ecology, resources, settlements, institutions, people, routes, trade, and historical events. Its starting state must be inspectable. The world continues into a deterministic expanding frontier as Jomon travels rather than ending at that initial generated region. Before a frontier region is materialized, persist only its deterministic regional commitments and any already revealed facts; materialize full people and mutable local state when a region or named person must exist.
- World generation aims for Dwarf Fortress-like depth and configurability, adapted to Jomon’s scale. Advanced settings cover region size, history length, climate, terrain and waterways, settlement and population density, political fragmentation, resource scarcity, ecology, dangers, era pace, and simulation fidelity. Players can select, save, inspect, and reproduce presets and advanced settings; every generated world records its seed, resolved settings, generator version, and validation/rejection result.
- Content-validation rules must reject sexual violence, slavery, torture, and harm/endangerment of children from generated history, people, places, events, contracts, hazards, rumours, and simulation summaries.
- Every instantiated person has persistent identity, location or home, role, material interests, family, work, needs, relationships, injury, possessions, birth, death, memories, and a readable history of consequential encounters.
- NPC actions must arise from original needs, opportunities, relationships, and local conditions—not an authored sequence disguised as simulation. Nearby and important people receive detailed simulation; every distant person retains full individual state and advances through deterministic scheduled summaries.
- The world must surface change through physical places, conversations, ledgers, rumours, goods, routes, visible work, and the always-visible-by-default management sidebar. The sidebar labels information source, discovery time, and freshness when relevant. These surfaces are complementary and must not merely duplicate one another. A simulation that players cannot discover or act upon is out of scope.
- Jomon expansion and exploration are the provisional long-term motivations. Every future vessel upgrade must add a physical space, a new material capability, or a meaningful new trade-off.
- Short-term goals must always be available through local pressures, contracts, favours, shortages, discoveries, threats, or crew needs. They guide play without creating a mandatory campaign finish line.
- Escalation must keep the world generative after NG++: later eras remix and extend the same systemic content families, change world conditions and relationships, and create new material problems, opportunities, and vessel choices. They do not require a new save or a campaign restart.
- Base, NG+, and NG++ are tracked world eras, not separate worlds. Accumulated in-world active-play time and Jomon’s growth advance the era; Jomon and surviving crew remain continuous while the wider world changes. NG++ plateaus in raw escalation and continues to remix systemic content instead of becoming numerical inflation.
- Shadow of Mordor is only a high-level reference for the feeling that remembered people can react and recur. Do not implement or market a “Nemesis System,” reproduce its hierarchy/vendetta design, or derive from its protected implementation. Use an original, documented social-memory model instead.

## Ordered implementation phases

### 0. Documentation reset `[x]`

- [x] Replace the old canon with `LORE.md` and establish root `TODO.md` as the governing delivery plan.
- [x] Remove superseded active plans and obsolete playtest documentation rather than reviving or archiving them as current direction.
- [x] Keep `README.md` truthful: the checked-in game is a prototype and the medieval implementation begins with a clean break.
- [x] Record Rogue-inspired terminal presentation, emergent-world direction, and non-copying boundaries as product constraints.
- [x] Supersede the no-magic setting constraint with the bounded low-mysticism direction in `LORE.md` and this plan.

Verification: manual repository review on 2026-09-01; documentation only. No build, test, or browser execution was run for this documentation slice.

### 1. Foundational world systems

#### 1.1 Clean game boundary `[x]`

- [x] Define the new medieval save namespace, version, and invalid-save behavior. Existing prototype saves are ignored, never migrated: only `jomon-medieval-worlds-v1` is opened, and malformed records resolve as unavailable.
- [x] Establish a clean `src/medieval/` root with renderer-independent foundation types for worlds, Jomon identity, deck partitions, quays, vessel props, crew, manifests, and zero-time action-clock seed state. It has no prototype gameplay/state imports.
- [x] Define a medieval browser bootstrap path, root state, routing boundary, and temporary generic-tooling adapters. `/` enters only medieval state; `?prototype` remains an explicitly isolated diagnostic path.
- [x] Define seeded game creation, a multi-world/chronicle index, an initial-courier selection surface, and read-only chronicle inspection/export. `MutableWorldSession` makes single-world mutation ownership explicit within a browser root.
- [x] Define the offline-first bootstrap contract in implementation: the medieval entry path has no account, remote telemetry, cloud, multiplayer, or network client; it uses local IndexedDB and packaged CSS only.
- [x] Remove prototype terminology from every current medieval user-facing canvas surface.
- [x] Add focused unit tests for clean creation, seed/manifest determinism, initial-courier selection, malformed-record invalidation/no migration, multiple-world indexing, finalized chronicles, and one-active-world ownership.
- [x] Run the authored Chromium browser test for the keyboard creation/select/save/resume flow and its no-external-request assertion. On 2026-09-01, `npx playwright test e2e/medieval-foundation.spec.ts` passed its one Chromium test after installing the local Playwright Chromium binary. The flow uses keyboard input to create, select, save, and resume a world, and asserts that no request leaves the local preview origin.

Acceptance: a fresh medieval world can be created and an initial courier selected deterministically; a prototype save cannot load as one; multiple local worlds can be indexed without loading each other; no network request or space-era term appears anywhere on the medieval new-game path.

Implementation design for this slice:

- Create `src/medieval/` as the only home for new medieval code. Start with `types.ts`, `rng.ts`, `world.ts`, `storage.ts`, `app.ts`, and colocated tests. Each domain module must be deterministic and renderer-independent; the app module is the only browser/UI coordinator.
- Use a distinct IndexedDB database named `jomon-medieval-worlds-v1`. Keep a small world index separate from active-world records and finalized chronicles. Never open, read, migrate, delete, or rewrite `jomon-expedition-v2` as part of medieval bootstrapping.
- Define a foundation manifest now—seed, resolved foundation configuration, generator version, creation metadata, and chosen initial courier ID—so Phase 1.2 can extend it without changing its identity. Create the foundation household deterministically from this manifest; the richer configurable geography/history pipeline remains Phase 1.2 work.
- Implement a minimal keyboard-first medieval bootstrap only: list local worlds and chronicles, create a seeded foundation world, choose from its generated eligible crew, resume one selected world, or inspect/export a finalized chronicle. It is a state/persistence proof, not the walkable deck, trade loop, combat, map renderer, or content slice.
- Change `src/entry.ts` to bootstrap the medieval app. Do not route ordinary users through the prototype. Prototype-specific browser tests must be replaced only by medieval coverage as the corresponding new path becomes implemented; direct prototype unit tests remain isolated until separately retired.
- Do not share `src/main.ts`, `src/renderer.ts`, `src/storage.ts`, `src/types.ts`, prototype content, prototype game state, or prototype save keys with the new domain. A small generic browser canvas or DOM shell may be rebuilt from first principles; no UI surface may expose prototype language.
- Verify this slice with new unit tests for manifests, generated crew, initial selection, catalog isolation, and save ownership; browser coverage for the keyboard creation/select/resume flow; `npm run build`; and `git diff --check`. Record any temporarily skipped legacy browser coverage plainly rather than treating it as medieval evidence.

#### 1.1.a Prototype reuse and disposition audit `[x]`

- [x] Classify every prototype foundation before reuse. Reuse only neutral tooling and the bare browser mount; rebuild all game-facing systems in `src/medieval/` from first principles.
- [x] Retain the Vite, TypeScript, Vitest, Playwright, package-lock, and generic `index.html` canvas-mount infrastructure. These provide build, type-check, test, and local browser boot capabilities only; they confer no medieval gameplay or visual requirements.
- [x] Do not reuse the prototype’s `src/main.ts`, `src/renderer.ts`, `src/storage.ts`, `src/types.ts`, `src/rng.ts`, `src/engine/**`, `src/world.ts`, `src/content.ts`, `src/props.ts`, autoplay/telemetry/campaign code, legacy browser flows, save keys, migrations, source art, sprite atlases, audio, or CSS. The sole approved font exception is the unmodified, separately attributed BigBlue Terminal 437 Nerd Font Mono, consumed only by the medieval stylesheet under its CC BY-SA 4.0 terms.
- [x] Isolate the legacy browser application behind `?prototype` so existing prototype browser tests can remain diagnostic without making it the ordinary product path. New browser coverage must open `/` and exercise only the medieval application.

Verification: manual import/dependency audit on 2026-09-01. `src/medieval/` has no prototype domain imports; `src/entry.ts` selects the medieval bootstrap by default and legacy bootstrap only with `?prototype`.

Detailed disposition record (completed 2026-09-01):

| Existing codebase area | Disposition for medieval Jomon | Why |
| --- | --- | --- |
| `src/main.ts`, `src/renderer.ts`, root UI state and `src/style.css` | Do not reuse | They encode the prototype's space-era screen structure, vocabulary, font, and visual identity. |
| `src/engine/**`, `src/world.ts`, `src/content.ts`, `src/props.ts`, `src/area-gates.ts`, `src/events.ts`, and adjacent progression/economy/escalation modules | Do not reuse | They encode the prototype's campaign, destinations, combat/content assumptions, and world model; importing them would create hidden medieval constraints. |
| `src/storage.ts`, root types/RNG/geometry/input/shared modules, and all save migrations | Do not reuse | Medieval persistence has a new database/schema and deterministic contracts; it must never load or rewrite `jomon-expedition-v2`. |
| `src/assets/**`, generated sprites, `src/audio.ts`, and legacy CSS | Do not reuse | They are prototype presentation/assets or carry an inherited visual/licensing identity; medieval gets original code-native ASCII presentation first. |
| `public/fonts/BigBlueTerm437NerdFontMono-Regular.ttf` | Reuse as a licensed exception | The user selected this local font as the medieval default. `src/medieval/style.css` imports it without changing the binary; `public/fonts/BigBlueTerm-NOTICE.txt` gives VileR and Nerd Fonts attribution, and the bundled CC BY-SA 4.0 license remains alongside it. |
| `src/autoplay*`, telemetry, campaign/balance reports, content validators, and product-specific scripts | Preserve only as legacy diagnostics; do not repurpose | Their metrics and fixtures assert a different game. Medieval gains its own focused fixtures as its systems exist. |
| `e2e/jomon.spec.ts` and legacy unit tests | Preserve behind `?prototype`; do not count as medieval verification | They protect unrelated existing work while the ordinary route is rebuilt. New coverage lives in `e2e/medieval-foundation.spec.ts` and `src/medieval/*.test.ts`. |
| Vite, TypeScript, Vitest, Playwright, package-lock, `vite.config.ts`, `playwright.config.ts`, and generic `index.html`/`#game` mount | Reuse | They are neutral local build, type-check, test, and canvas-boot infrastructure only. `src/vite-env.d.ts` restores the standard Vite CSS-module declaration after the split. |

Verification: on 2026-09-01, `npx vitest run src/medieval/world.test.ts src/medieval/storage.test.ts src/medieval/session.test.ts --maxWorkers=1 --no-file-parallelism` passed 10 tests; `npx playwright test e2e/medieval-foundation.spec.ts` passed 1 Chromium browser test; `npm run build` and `git diff --check` passed. Playwright Chromium, its headless shell, and its FFmpeg support were installed in the user Playwright cache before the browser run. npm reported an existing unsupported `email` configuration warning; no verification command failed.

Licensed terminal-font and wrapping correction (2026-09-01): the medieval stylesheet now registers the local BigBlue Terminal 437 Nerd Font Mono file as `BigBlueTerm`, and the canvas explicitly draws with that family after the browser font is ready. Canvas text uses measured, word-aware wrapping inside the panel, including long seed text, generated histories, causal records, errors, and explanatory copy; a long unbroken token is split only when necessary. The source font is VileR’s BigBlue Terminal, distributed as CC BY-SA 4.0 and patched by Nerd Fonts. The project retains the full license at `public/fonts/BigBlueTerm-LICENSE.txt` and adds the required attribution, source links, license link, and modification status at `public/fonts/BigBlueTerm-NOTICE.txt`; the bundled binary is unmodified.

Verification for the licensed font and wrapping correction: on 2026-09-01, `npm exec vitest -- run src/medieval/content-safety.test.ts src/medieval/world.test.ts src/medieval/storage.test.ts src/medieval/session.test.ts src/medieval/generation-config.test.ts` passed 28 tests; `npm run build`, `npx playwright test e2e/medieval-foundation.spec.ts` (1 Chromium test, including the BigBlueTerm readiness assertion), and `git diff --check` passed. A 2048×982 Chromium screenshot was manually inspected and confirms the roadmap line wraps inside the canvas panel. npm reported its existing unsupported `email` configuration warning, and Playwright reported the existing `NO_COLOR`/`FORCE_COLOR` environment warning; neither command failed.

#### 1.2 Deterministic world generation and configuration

- [x] Define a versioned `WorldGenerationConfig`, named presets, advanced settings, validation constraints, and deterministic rejection/retry rules. `src/medieval/generation-config.ts` provides pure resolution, rejection diagnostics, and a four-attempt deterministic candidate plan with focused coverage.
- [x] Define controls for region size, history length, climate, terrain and waterways, settlement and population density, political fragmentation, resource scarcity, ecology, dangers, era pace, and simulation fidelity; preserve every selected and resolved value in the manifest. `WorldManifest` v5 retains canonical selected settings, their full resolution, normalized seed, versioned generator/policy/frontier contracts, deterministic candidate streams and diagnostics, initial-world/frontier identities and digests, and content-safety provenance.
- [x] Define an enforceable content-boundary taxonomy and deterministic validation/rejection rules that exclude sexual violence, slavery, torture, and harm/endangerment of children from all generated world history and content. `src/medieval/content-safety.ts` supplies policy v1, canonical structured classifications, stable rejection codes, and fail-closed audit validation; foundation creation and local save validation apply it to generated labels, Jomon, initial-world records, people, histories, events, and player-facing text.
- [x] Build the dependency-ordered initial-world pipeline: watershed geography and hydrology; climate and seasons; resources and ecology; settlement sites; institutions and people; then routes, trade, and pre-play history. `src/medieval/initial-world.ts` now creates a bounded, renderer-independent foundation-region graph in that exact order and accepts only the first valid candidate from the existing four-attempt plan.

Implementation design for the completed initial-world pipeline:

- `src/medieval/initial-world.ts` owns immutable initial-world records, the ordered candidate stages, validation, safety-record collection, and bounded candidate selection; `world.ts` owns foundation assembly/recreation, `types.ts` owns manifest/world references, and `storage.ts` accepts only a structurally valid world whose initial-world output and manifest diagnostics reproduce exactly.
- The generated record graph is watershed/waterways → climate/seasons → resources/ecologies → settlements → institutions and person seeds → routes/trade/history. Records use stable `initial:` IDs; downstream fields are IDs only, and the validator rejects missing, duplicate, out-of-stage, or non-viable references. Person seeds are generation/history participants only, not Phase 1.3 mutable people.
- Each retry candidate starts from the existing `GenerationAttempt.streamSeed`; every stage creates its own named `SeededRng` stream from that value. Stages receive only resolved configuration, explicit upstream records, and that stream. No stage reads time, network, globals, or another stage’s RNG state.
- `InitialWorldGenerationDiagnostics` records each attempted candidate’s stream, ordered validation issues, and selected attempt. The pipeline selects the first valid member of the four existing attempts; a valid configuration whose four candidates fail throws a bounded exhaustion error carrying those diagnostics. The successful diagnostics and initial-world output are reproduced from the manifest.
- Every resolved configuration field has a direct output effect: region span, history horizon, climate/season profile, relief, waterways, settlement count, population band, institutional plurality, resource availability, ecology variety, route pressures, seasonal pace, and static generation detail respectively. Per-stage limits bound all collections.
- Every generated named or player-visible record supplies the existing content-safety classification. Candidate validation gathers those records through the policy validator and rejects missing, unsafe, or unclassified content before it reaches a world or local save.
- Focused tests cover deterministic reproduction, stage/reference causality, each configuration effect, first-valid/rejection/exhaustion behaviour, safety rejection, bounds, manifest recreation, and local-record invalidation. No browser surface, frontier, action clock, simulation, map, or trade gameplay is part of this slice.

- [x] Define the deterministic expanding-frontier contract: regional coordinates, persistent identities, pre-arrival knowledge/rumours, causal links to known regions, region generation order, regional commitments before materialization, named-person instantiation, and no-contradiction guarantees. `src/medieval/frontier.ts` now provides the pure, bounded contract and validation layer; it does not introduce travel, simulation, map rendering, or a save-schema change.

Implementation design for the completed expanding-frontier contract:

- `src/medieval/frontier.ts` owns the versioned, renderer-independent frontier state, coordinate/identity helpers, regional-commitment and materialization planners, knowledge reducers, no-contradiction validator, and content-safety record collection. It consumes `InitialWorld` and resolved configuration as immutable inputs without changing the v4 world manifest or storage schema; the following manifest task chooses its durable envelope.
- A region has an integer basin coordinate and a stable ID derived from the initial-world ID plus that coordinate. It is committed in a total generation order and has either an initial-world causal anchor or an earlier parent-region connection. Initial anchors must cite real watershed, waterway, settlement, route, trade, climate, ecology, and history IDs; child commitments inherit that grounded link and may not be disconnected biomes.
- `ungenerated` regions contain only bounded anonymous population/role/institution commitments and connection metadata. `known-but-unvisited` additionally retains immutable source-labelled facts with supplied world-time/freshness fields and optional named-person commitments. `materialized` retains a deterministic static site/materialization plan and stable future-person instantiation plans, but creates neither a map nor Phase 1.3 mutable people, simulation records, travel time, or routes.
- Each commitment, name, fact, rumour source, anonymous template, and materialized record carries the existing structured content-safety classification. Validation is fail-closed and canonically ordered: it rejects unknown/missing classifications, unsafe policy metadata, duplicate identities, invalid anchors, invalid parent/order links, over-budget state, malformed source timing, and a second claim with a different value for the same subject/fact key.
- Frontier creation and each derived commitment/materialization use named `SeededRng` streams based only on the normalized seed, resolved configuration fingerprint, initial-world ID, coordinate, and operation key. Knowledge application is a pure immutable reducer: it validates before accepting a fact, is idempotent for an identical stable fact ID, and leaves all time advancement to a later action-clock caller.
- Focused tests cover coordinate and ID stability; rooted causal links; deterministic materialization; immutable pre-arrival knowledge; name/future-person stability; contradiction and safety rejection; and commitment/fact/person bounds. Browser UI, font/CSS, map rendering, actual travel, persistent person simulation, and IndexedDB manifest persistence are outside this slice.

- [x] Record a reproducible world manifest containing seed, resolved configuration, generator version, initial and frontier region identifiers, and validation/rejection history. `WorldManifest` v5 is a canonical immutable creation record plus bounded mutable courier state; it reconstructs and validates the initial-world and frontier-root evidence before local storage accepts it.

Implementation design for the completed reproducible-manifest contract:

- `types.ts` will make manifest v5 an immutable `creation` provenance record plus the existing bounded mutable courier-selection field. `world.ts` will create, canonicalize, validate, digest, and reconstruct that record; `storage.ts` will continue to accept only a fully reproducible medieval world. `initial-world.ts`, `frontier.ts`, and `content-safety.ts` remain the owners of their domain contracts and supply only their public deterministic data.
- Creation provenance will canonically retain the normalized seed; selected/resolved configuration and fingerprint; foundation, initial-world, frontier, and safety-policy versions; the exact four candidate streams and ordered candidate diagnostics; an initial-world digest and bounded stable-ID index; and a bounded frontier-root commitment index/digest. It intentionally records neither a mutable frontier state, latent people, nor player discoveries.
- The manifest builder will derive the initial world and frontier roots from the recorded seed/configuration, audit their classified content, and calculate deterministic digests over fixed-shape, canonically ordered data. Reconstruction will consume the nested provenance itself, regenerate the same records, and reject changed versions, diagnostics, ordering, IDs, references, or digest/audit data before a world is restored.
- A v4-or-earlier medieval manifest is a clean-break incompatibility: it lacks the frontier-root and canonical creation evidence required here, so storage will reject it rather than infer, migrate, or touch any prototype save. The database namespace and outer world envelope remain medieval-only.
- Focused tests will cover canonical serialization, immutable reconstruction equivalence, initial/frontier identity stability, candidate rejection-history integrity, malformed/tampered provenance rejection, and medieval-storage isolation. No UI, travel, simulation, mutable frontier exploration, or map work is part of this slice.

Completed reproducible-manifest decision (2026-09-01): `WorldManifest` v5 separates immutable `creation` provenance from the bounded mutable `initialCourierId` and its current-state safety audit. Creation canonically records the normalized seed, exact selected/resolved configuration and fingerprint, foundation/initial-world/frontier/content-policy versions, the four retry streams and ordered initial-world candidate diagnostics, a full bounded initial-world stable-ID index and reproducibility digest, plus a bounded frontier-root commitment index and digest. The roots retain coordinates, order, streams, anchors, and connections, but retain neither materialized regions, named people, nor discovered facts.

Creation generates and audits the initial world and frontier roots before deriving both the creation digest and foundation-world ID; reconstruction consumes that record to regenerate and compare every canonical field. Validation rejects changed contract versions or safety provenance, malformed/reordered diagnostics, altered digests, noncanonical root order, and initial/frontier identity mismatches. v4 and older medieval manifests are intentionally rejected without migration because they cannot evidence this frontier contract; the medieval-only IndexedDB namespace and outer foundation-world envelope remain separate from every prototype save.

Verification for the completed reproducible-manifest contract: on 2026-09-01, `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 49 tests in 8 files. `npm run build` passed (Vite transformed 149 modules and the bundle-size check passed), and `git diff --check` passed. npm emitted the existing unsupported `email` configuration warning; no required final command failed. Playwright was not run because this slice preserves browser-visible behaviour.

#### 1.2.a Dwarf Fortress Classic-inspired sixteen-colour palette `[x]`

This intermediate visual task deliberately precedes the settings UI so every forthcoming creation/control surface shares one small, testable palette contract. It implements only colour and colour-dependent legibility—not a map, glyph vocabulary, settings screen, world-generation behaviour, or detailed renderer.

- [x] Define a versioned, renderer-independent palette module with the sixteen historical Dwarf Fortress Classic console colour values and dark/bright pairs: black/dark gray; blue/light blue; green/light green; cyan/light cyan; red/light red; magenta/light magenta; brown/yellow; and light gray/white. Use [the Dwarf Fortress Classic default scheme](https://dwarffortresswiki.org/index.php/Color_scheme) as the value reference, while Jomon owns its colour-role names and assignments.
- [x] Replace all current medieval canvas and stylesheet colour literals with semantic Jomon palette tokens. The foundation screen uses only the approved sixteen colours for its background, panel, border, body text, muted information, selected entry, action prompt, error state, and reserved future water/route/status roles.
- [x] Preserve non-colour readability: selection, urgency, and state retain textual, glyph, or layout cues; no consequential current or future terminal state is distinguished by colour alone. Contrast and dark/bright pairing remain deliberate on the black console ground.
- [x] Add focused unit tests for exact palette membership/pairing, semantic-token completeness, and non-colour status cues. Add browser coverage and a manually inspected screenshot of the foundation screen confirming the actual rendered palette and readable wrapping at the existing reference viewport.
- [x] Document the palette as a Jomon presentation contract and a Dwarf Fortress Classic visual influence only. Do not copy Dwarf Fortress glyphs, layout, UI vocabulary, data, art, or gameplay semantics, and do not add a player palette-customisation UI in this task.

Completed sixteen-colour palette decision (2026-09-01): `src/medieval/palette.ts` owns palette v1 as serializable RGB/hex data for the historical pre-v50 default values and their eight ordered pairs. `JOMON_PALETTE` maps that finite set to original Jomon console-ground, panel, text, selection, action, warning/error, water, route, and status roles; `JOMON_NON_COLOR_STATE_CUES` specifies the accompanying marker/labels. `main.ts` applies generated CSS custom properties from the same module, while `app.ts` and `style.css` consume semantic tokens only. BigBlue Terminal, its attribution/notice, keyboard behaviour, wrapping, and canvas dimensions were unchanged.

Verification for the completed sixteen-colour palette: on 2026-09-01, `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 53 tests in 9 files; `npm run build` passed (Vite transformed 150 modules and the bundle-size check passed); `npx playwright test e2e/medieval-foundation.spec.ts` passed 1 Chromium test including font readiness, palette CSS variables, and a black canvas sample; and `git diff --check` passed. `npx playwright screenshot --viewport-size="2048,982" --wait-for-selector="#game" --wait-for-timeout=500 http://127.0.0.1:4173 /tmp/jomon-medieval-palette-2048x982.png` captured a temporary screenshot that was manually inspected: the black ground, gray rules/border, yellow headings, green actions, and wrapped copy are legible. A duplicate `npm run preview -- --host 127.0.0.1 --port 4173 --strictPort` could not start because the already-running local preview owned port 4173; it did not block the browser or screenshot verification. npm emitted the existing unsupported `email` warning and Playwright emitted the existing `NO_COLOR`/`FORCE_COLOR` warning; no required check failed. No screenshot artefact was added to the repository.

Acceptance: the playable medieval foundation route visibly uses the coherent sixteen-colour console scheme; every rendered medieval colour comes from the palette contract; selection and error state remain readable without colour; automated/browser checks and a visual inspection are recorded.

#### 1.2.b Settings UI and generation diagnostics

- [x] Add settings UI for preset selection, advanced configuration, seed entry/display, world-creation progress, result inspection, and saving/loading settings.

Implementation design for the active settings UI and generation-diagnostics slice:

- `src/medieval/settings.ts` will own versioned, serializable creation-setting drafts, normalized seeds, named-profile validation, and the six-profile cap. It delegates every preset/advanced resolution and rejection to `generation-config.ts`; it owns no parallel configuration rules. `storage.ts` will add one medieval-only `creation-settings` record in a version-2 upgrade of the existing medieval database, reject malformed records to defaults, and replace an existing canonical profile name in place while refusing a seventh distinct profile.
- The keyboard canvas will use settings, profile, generation-progress, and result-inspection routes before the existing courier-choice route. The basic page edits seed/preset and opens advanced/profile pages; the advanced page exposes every configured field through options exported by `generation-config.ts`; the profile page saves/loads exact drafts. Seed normalization is displayed explicitly, invalid selections show the resolver diagnostics and cannot create a world, and loaded/saved setting data never mutates an existing world or its manifest.
- Initial-world generation will expose a synchronous, deterministic trace for the actual six causal stages plus validation and accepted/rejected candidate outcome. Creation records those events while it executes the generator, then presents a progress ledger followed by paged result inspection of seed, selected/resolved configuration, candidate diagnostics, bounded initial-world counts, immutable manifest IDs/digests, and the generated crew. The trace schedules no timer, uses no new RNG, and advances no world time.
- The palette module remains the sole colour authority. Each setting selection has the existing `>` marker, action key text, explicit validity/error labels, and descriptive canvas aria text, so state never depends on colour. BigBlue Terminal, wrapping, canvas size, prototype isolation, and existing world/courier flow remain intact.
- Focused tests will cover setting resolution and immutable-manifest separation; local last-used/profile persistence, invalid-record rejection, cap, and replacement; actual stage trace; and a keyboard browser path that changes a preset/advanced field, saves and reloads a profile, generates/inspects a world, selects a courier, resumes it, and observes no external request.
- [x] Add generation snapshots and property/fuzz tests for determinism, valid geography-to-settlement dependencies, constrained settings, content-boundary rejection, expanding-frontier continuity, bounded generation, and readable diagnostics.

Completed generation snapshots and property/fuzz testing decision (2026-09-01): `generation-snapshots.test.ts` fixes two intentionally small, readable projections: `snapshot-reed-17` with the watershed preset and `snapshot-coast-43` with documented far-coast overrides. Each projection records the normalized seed, world/manifest identifiers, selected configuration, accepted candidate and issue codes, bounded stage counts, representative settlement/water/resource/ecology and route/history edges, frontier region coordinates/connections plus the chart fact, and two generated crew records. They are direct in-test fixtures rather than opaque serialized-world dumps, so a change reports a meaningful contract difference.

`generation-properties.test.ts` uses installed `fast-check` with deterministic seeds `20260901`–`20260906`, verbose shrinking, and bounded ordinary-development run counts: 24 world/manifest/frontier reproductions, 48 valid all-control configurations, 64 malformed/incompatible configurations, 24 safety injections, 32 frontier cases, and 20 forced retry exhaustions. It covers initial-world graph/budget invariants across every preset and two representative advanced extremes; exact repeated manifest/frontier reconstruction; closed invalid configuration diagnostics; accepted content audits plus prohibited/unclassified rejection; coordinate/identity/anchor stability, idempotent knowledge, materialization, and contradiction rejection; and four-attempt canonical exhaustion diagnostics. A malformed resource correctly reports both its own invalid record and any downstream invalid settlement, so the exhaustion assertion requires stable bounded causal diagnostics instead of hiding that dependency.

No production contract changed: the existing resolver, generator, manifest, content-safety policy, and frontier functions already supplied the necessary public invariants. The browser UI, font, palette, local storage, and prototype boundary were not changed.

Verification for the completed generation-test slice: on 2026-09-01, the initial `npx vitest run src/medieval/generation-snapshots.test.ts src/medieval/generation-properties.test.ts --maxWorkers=1 --no-file-parallelism` execution failed only because the new exhaustion assertion incorrectly expected one diagnostic rather than the validator’s deliberate resource-plus-dependent-settlement result; the assertion was corrected without changing production code. The same focused command then passed 8 tests in 2 files, and the required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 69 tests in 12 files. `npm run build` passed (Vite transformed 151 modules and the bundle-size check passed), and `git diff --check` passed. npm emitted the existing unsupported `email` configuration warning; all final required checks passed. Playwright was not run because this slice makes no browser-visible production change.

Completed settings UI and generation-diagnostics decision (2026-09-01): `settings.ts` owns v1 canonical local creation drafts (normalized seed, selected generation configuration, and advanced-mode preference), profile validation, and a six-profile cap; `generation-config.ts` remains the only resolver and source of selectable advanced values. The canvas basic settings page offers seed entry with explicit normalized display, preset cycling, advanced/settings-profile routes, and creation. Advanced settings exposes all thirteen resolved controls—region size, history years, climate, terrain ruggedness, waterway density, settlement and population density, political fragmentation, scarcity, ecology, danger, era pace, and simulation fidelity—with `*` marking explicit overrides and `R` returning a field to its preset value. Resolver diagnostics prevent creation rather than silently repairing invalid combinations.

The medieval-only IndexedDB database now uses a version-2 upgrade with one `creation-settings` record, separate from catalogues, worlds, and chronicles and never from a prototype namespace. It loads a malformed record as the documented default instead of interpreting it; it records the last successfully saved/created draft; and it permits at most six named profiles of 1–32 normalized characters. A matching profile name replaces its existing position, while a seventh distinct name is rejected. Loading or changing settings never changes any existing immutable world manifest; creating a new world passes the canonical selected settings through the existing resolver and manifest provenance contract.

The creation route records an actual deterministic initial-world trace: the six generator stages (watershed/hydrology, climate/seasons, resources/ecology, settlements, institutions/people, routes/trade/history) followed by candidate validation and its accepted/rejected result. It uses no timer, unseeded work, or world-time increment. After the trace, result pages expose the normalized seed, selected preset/overrides and full resolved configuration, candidate diagnostics, initial-region counts, immutable world/initial/frontier identifiers and digests, policy/contract versions, and generated crew before the existing courier confirmation. Keyboard focus, `>` selection, `+` readiness, explicit labels/errors, canvas aria text, measured wrapping, BigBlue Terminal attribution/setup, and semantic palette tokens remain intact.

Verification for the completed settings UI and generation diagnostics: on 2026-09-01, `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 61 tests in 10 files; `npx playwright test e2e/medieval-foundation.spec.ts --project=chromium` passed 1 Chromium keyboard-flow test covering preset/advanced edits, seed normalization, profile save/load, creation progress/result inspection, courier selection, page reload/resume, palette/font readiness, and no external requests; `npm run build` passed (Vite transformed 151 modules and the bundle-size check passed); and `git diff --check` passed. A fresh 1280×720 temporary screenshot at `/tmp/jomon-medieval-settings-final.png` was manually inspected: the advanced configuration fits the fixed canvas, uses readable wrapping/cues, and retains the approved black/gray/yellow/cyan/green console presentation. No screenshot artefact was added. A separate `npm run preview -- --host 127.0.0.1 --port 4173 --strictPort` invocation could not bind because an already-running local preview held port 4173; it did not block Chromium or screenshot verification. npm emitted the existing unsupported `email` configuration warning and Playwright emitted the existing `NO_COLOR`/`FORCE_COLOR` warning; no required command failed.

Acceptance: the same seed and resolved configuration reproduce the same initial world and explored frontier; changing a documented setting predictably changes the intended world property; prohibited content is absent from generated output; a player can inspect, share, and restore the world manifest.

Completed expanding-frontier contract decision (2026-09-01): `frontier.ts` defines contract v1 as a pure state machine keyed by a normalized seed, resolved-configuration fingerprint, and initial-world ID. Integer basin coordinates deterministically derive stable region IDs and named streams. Root and child commitments have a total, contiguous generation order; roots cite real initial watershed, waterway, settlement, route, trade, climate, ecology, and history records, while children inherit that anchor and cite an earlier parent connection. Three initial commitments demonstrate upstream, chart-known coastward, and parent-linked coastward expansion without disconnected biomes; subsequent adjacent commitments remain capped at 12.

An `ungenerated` region has only anonymous adult household, role, institution, anchor, and connection commitments. A `known-but-unvisited` region adds immutable source-labelled facts whose reported, freshness, and known world times are supplied by a future action-clock caller; a `materialized` region adds a deterministic static site and future-person instantiation plans only. The contract creates no mutable people, no world-time updates, no travel routes, no map, and no persistence writes. A revealed named person has a seed/region/revelation-key-derived commitment ID and future persistent-person ID; its immutable name fact, role, anonymous institution, and `region-materialized` trigger are reproduced exactly. Site, route, history, person-role, and person-relationship facts use stable subject IDs and cannot change a prior subject/fact value.

Validation rejects malformed context, noncanonical identity/order/anchor/connection links, budget excess, invalid fact subjects or source timing, changed claims, invalid name/materialization plans, and all unclassified or unsafe content. It uses the existing policy audit for regional data, anonymous templates, source labels, facts/rumours/history links, names, and materialized plans. The state reducer is immutable and idempotent for an identical fact or name revelation; materialization has a named stream independent of its recorded timestamp so equivalent source world/facts always produce the same static region details. The v4 manifest and local storage remain unchanged until the newly active manifest-record task determines a durable frontier envelope. The BigBlue Terminal font, attribution, wrapping correction, and browser path were untouched.

Verification for the completed expanding-frontier contract: on 2026-09-01, `npx vitest run src/medieval/frontier.test.ts src/medieval/initial-world.test.ts src/medieval/world.test.ts src/medieval/storage.test.ts src/medieval/content-safety.test.ts src/medieval/generation-config.test.ts src/medieval/session.test.ts --maxWorkers=1 --no-file-parallelism` passed 42 tests in 7 files. `npm run build` passed (Vite transformed 148 modules and the bundle-size check passed), and `git diff --check` passed. npm emitted the existing unsupported `email` configuration warning; no required command failed. Playwright was not run because this slice changes no browser-visible behaviour.

Completed initial-world pipeline decision (2026-09-01): `src/medieval/initial-world.ts` owns the immutable, versioned initial-region graph, six ordered stage functions, candidate validation, policy-record collection, and bounded candidate selection. `world.ts` assembles and recreates the foundation world; `types.ts` owns the v4 manifest/world references; `storage.ts` rejects records whose initial region or generation provenance cannot be reproduced exactly. The graph uses stable `initial:` IDs and validated references from watershed/waterways through climate/seasons, resources/ecologies, viable settlements, institutions and non-mutable person seeds, routes/trade links/hazards, and history events. Person seeds are generation/history participants only; Phase 1.3 persistent people, fidelity scheduling, and the action clock remain unimplemented.

Every candidate stage receives only the resolved configuration, explicit upstream records, and its own named `SeededRng` stream derived from the existing attempt stream. The successful pipeline records diagnostics v1: its generator and content-policy versions, selected attempt, and ordered attempted-candidate diagnostics. It rejects duplicate IDs, invalid stage references, non-viable settlement water/resource links, out-of-bounds output, configuration mismatches, and missing/unsafe/unclassified content. The generator selects the first valid candidate; bounded exhaustion returns all four recorded rejections without altering configuration or drawing non-seeded entropy. The v4 clean-break manifest persists these diagnostics while the existing safety audit persists the classified initial-world records; local saves additionally require the actual initial-world output to match the manifest reconstruction.

The resolved controls directly set region span, history horizon/event count, climate profile, relief, waterway count, settlement count, population band/person-seed count, institutional plurality, resource count/availability, ecology count, route-hazard pressure, seasonal pace, and static generation detail. Foundation caps are 7 waterways/resources, 4 seasons, 5 ecologies/routes/trade links/route hazards, 6 settlements, 30 institutions, 60 person seeds, and 8 history events. No browser surface, expanding frontier, simulation, map, trade gameplay, or settings UI was changed; the existing BigBlue Terminal font attribution and wrapping correction remain untouched.

Verification for the completed initial-world pipeline: on 2026-09-01, `npx vitest run src/medieval/initial-world.test.ts src/medieval/world.test.ts src/medieval/storage.test.ts src/medieval/content-safety.test.ts src/medieval/generation-config.test.ts --maxWorkers=1 --no-file-parallelism` passed 34 tests, and the final `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 35 tests in 6 files. `npm run build` passed (Vite transformed 148 modules and the bundle-size check passed), and `git diff --check` passed. npm emitted the existing unsupported `email` configuration warning; no required command failed. Playwright was not run because this slice changes no browser-visible behaviour.

Completed configuration-contract decision (2026-09-01): `WorldGenerationConfig` v1 resolves one named preset plus explicit advanced overrides before any generation work. Its current preset defaults are:

| Preset | Region / history / climate | Terrain / waterway | Settlement / population | Fragmentation / scarcity / ecology / danger | Era / fidelity |
| --- | --- | --- | --- | --- | --- |
| `sheltered-reach` | `compact` / 200 years / `temperate` | 2 / 4 | 3 / 2 | 2 / 2 / 3 / 2 | `measured` / `focused` |
| `watershed` (default) | `standard` / 300 years / `temperate` | 3 / 4 | 3 / 3 | 3 / 3 / 3 / 3 | `measured` / `balanced` |
| `far-coast` | `broad` / 400 years / `cool-wet` | 4 / 3 | 3 / 3 | 4 / 4 / 4 / 4 | `brisk` / `deep` |

`regionSize` is `compact`, `standard`, or `broad`; `historyYears` is an integer from 100 through 600 in 25-year steps; `climate` is `cool-wet`, `temperate`, or `warm-dry`; `eraPace` is `measured`, `brisk`, or `pressing`; and `simulationFidelity` is `focused`, `balanced`, or `deep`. Terrain ruggedness, waterway density, settlement density, population density, political fragmentation, resource scarcity, ecology complexity, and danger pressure are integer levels 1–5. Inputs are never coerced or silently repaired: unknown/malformed settings and incompatible combinations are rejected with field diagnostics. Population density may be at most one level above settlement density, and a broad region requires at least 200 history years. Future generators must evaluate exactly four seed/config-derived candidates in attempt order 0–3, select the first valid result, and report bounded exhaustion if all are rejected; retries cannot alter the configuration or use non-seeded entropy.

Verification for the completed configuration contract: on 2026-09-01, `npx vitest run src/medieval/world.test.ts src/medieval/storage.test.ts src/medieval/session.test.ts src/medieval/generation-config.test.ts --maxWorkers=1 --no-file-parallelism` passed 15 tests; `npx playwright test e2e/medieval-foundation.spec.ts` passed 1 Chromium browser test; and `npm run build` passed. npm reported the existing unsupported `email` configuration warning, but no command required by this medieval slice failed. The active manifest-preservation task has not changed the foundation manifest or creation UI yet.

Broader diagnostic verification on 2026-09-01: legacy `npm run test:autoplay:tasks` passed. Full `npm run test:e2e` ran 9 tests: the medieval foundation test and three legacy diagnostics passed, while five legacy `e2e/jomon.spec.ts` flows failed after their prototype route remained `loading` instead of reaching `approach` within 30 seconds. These legacy failures do not invalidate the completed medieval browser test and are not medieval feature evidence. `npm test` was started but its legacy Vitest matrix remained idle for more than six minutes in one serial invocation (0% CPU, no further output), so that diagnostic run was terminated and is recorded as incomplete rather than passing.

Completed manifest-provenance decision (2026-09-01): the manifest schema began independently versioned at v3 and is now v4 for the verified initial-world pipeline, while the clean medieval IndexedDB namespace and outer foundation-world envelope remain unchanged. Creation resolves the requested configuration before it creates a manifest, derives the world ID, label, foundation-household RNG stream, and initial-world candidates from that resolved configuration, and records both the canonical selection and exact resolved values. A persisted manifest is accepted only when its selection resolves to its recorded configuration, its accepted empty validation record and four retry streams match the normalized seed/configuration, its label and initial-world candidate diagnostics match, its policy audit matches the generated classified records, and its world ID matches; otherwise it is unavailable. This is a fail-closed replacement for incomplete earlier medieval manifest records, not a migration, and it never opens or changes any prototype save. `recreateFoundationWorld` rebuilds the same zero-time world from a valid manifest's normalized seed and selection.

Verification for the completed manifest-provenance contract: on 2026-09-01, `npx vitest run src/medieval/generation-config.test.ts src/medieval/world.test.ts src/medieval/storage.test.ts --maxWorkers=1 --no-file-parallelism` passed 18 tests; `npm run build` and `git diff --check` passed. The browser test was not rerun because this slice does not alter a browser UI path; its completed Phase 1.1 Chromium result remains recorded above. npm reported the existing unsupported `email` configuration warning, but no command required for this slice failed.

Completed content-boundary decision (2026-09-01): `src/medieval/content-safety.ts` defines medieval content-safety policy v1. Every generated record passed to the policy supplies a primary domain plus a canonical structured classification: the classified domains, adult-only or not-applicable participant scope, approved descriptive tags, and an exact exclusion entry for sexual violence, slavery, torture, and child harm or endangerment. Missing, unknown, incomplete, or noncanonical classification data rejects with stable codes; no keyword blacklist is treated as the authority. The taxonomy explicitly covers history, people, places, events, contracts, hazards, rumours, enemy behaviour, simulation summaries, player-facing text, and future templates/data. `WorldManifest` v4 persists an accepted policy audit (including policy version and classified-record snapshots). Foundation world creation creates and audits classified label, Jomon, crew, initial-world records, history, and causal-event records deterministically; save/load validation rejects an altered record or audit, and manifest recreation requires the same provenance snapshot. This does not migrate or open prototype storage.

Verification for the completed content-boundary contract: on 2026-09-01, `npm exec vitest -- run src/medieval/content-safety.test.ts src/medieval/world.test.ts src/medieval/storage.test.ts src/medieval/generation-config.test.ts` passed 27 tests; `npm run build` and `git diff --check` passed. Browser tests were not run because this slice does not alter browser-visible behaviour. npm reported the existing unsupported `email` configuration warning, but no required command failed.

#### 1.3 Active-play world simulation and persistence kernel

- [x] Define the action-driven simulation clock, event ordering, seeded random streams, and deterministic scheduler. Only state-changing in-world commands advance time; pure UI inspection and idle/paused/closed browser time must be zero-time.

Implementation design for the completed action-clock and scheduler slice:

- `src/medieval/temporal.ts` will own temporal contract v1: integer in-world minutes, a zero-time clock, action sequence, bounded pending-event queue, canonical due-event results/causal records, command validation, and pure state transitions. `world.ts` will bind that state to immutable creation provenance and append its causal records; `types.ts` will own the versioned mutable envelope; `storage.ts` will reject any temporal state that does not reproduce those contracts. No renderer, browser timer, or gameplay system owns simulation authority.
- Time-bearing action inputs are the closed future-compatible families `movement`, `wait`, `travel`, `work`, `rest`, `delegated-task-commitment`, and `delegated-task-resolution`. Each supplies a positive, safe integer minute duration within its documented family bound. Inspection, settings, routes, browser idle/pause/reload, and arbitrary UI inputs are explicit pure command kinds and are rejected by the advancing reducer with no partial mutation.
- A pending event has a stable ID, non-past due minute, closed priority, scheduler-assigned sequence, source-action/provenance payload, and content-safety classification. Due events sort strictly by due minute, priority, ID, then sequence; requested events are canonically sorted before sequences are assigned, so insertion order cannot diverge state. An action advances the clock once, then resolves every due event in that total order and emits bounded, inspectable causal results.
- Random values use fresh `SeededRng` instances from named temporal stream seeds derived only from temporal-contract version, immutable world ID/creation digest/normalized seed, stream name, and action or event ID. There is no mutable RNG state, wall clock, timer, network, or hidden global input. The initial temporal state has no pending events, causal records, or RNG state.
- Validation fails closed with stable diagnostics for malformed provenance/state/payload, pure commands, non-positive or out-of-bound duration, unsafe time or sequence overflow, invalid/noncanonical priority/order, duplicate action/event IDs, past scheduling, over-cap queue/history, and unsafe or unclassified action/event records. The queue cap is 32 events and the causal-record cap is 256; content safety audits cover both pending and resolved scheduler records.
- `FoundationWorld` will become mutable-envelope v2 while `WorldManifest` v5 remains unchanged immutable creation provenance. Creation and manifest reconstruction produce an exact empty temporal state; save/load preserves validated later temporal state and verifies that its causal projection matches the world history. Existing medieval v1 world/chronicle envelopes are rejected without migration because their lone zero-time field cannot prove action sequence, queue, event ordering, or scheduler safety data; prototype storage remains untouched.
- Focused tests will cover zero-time creation/reconstruction, explicit action advancement, pure-command/idle/reload invariants, named-stream and replay stability, order independence, invalid inputs, persisted pending-event equivalence, queue/time bounds, content-safety rejection, and the old-envelope clean break. No fidelity, distant simulation, people, era, travel, UI, map, trade, or combat behaviour is implemented here.

Completed action-clock and scheduler decision (2026-09-01): `src/medieval/temporal.ts` owns temporal contract v1. Its stable unit is the integer in-world minute; creation and manifest reconstruction start at minute zero with no actions, pending events, causal records, or mutable RNG cursor. The closed time-bearing families are movement (1–60 minutes), wait/work/rest (1–1,440), travel (1–10,080), and delegated-task commitment/resolution (1–720). A valid action explicitly supplies one bounded duration; `inspect`, settings open/close, route change, browser idle/pause/reload, and arbitrary UI commands are explicit pure kinds that the advancing reducer rejects without changing any temporal value.

Pending events retain a stable ID, due minute, `urgent`/`ordinary`/`deferred` priority, canonical scheduler sequence, source-action/creation-digest payload, and structured content-safety classification. Requested events sort before sequence assignment; all due events then resolve by due minute, priority, ID, sequence. The reducer advances one action and resolves every due event at the first qualifying action, recording deterministic random result values and readable causal records. It bounds pending events at 32 and scheduler causal records at 256, rejects unsafe integer overflow, and validates duplicate IDs, noncanonical order, past events, malformed payload/provenance, and unsafe/unclassified action or event data with stable diagnostics. A fresh named `SeededRng` stream is derived from temporal-contract version, immutable world ID, creation digest, normalized seed, named stream, and action/event ID; there is no wall-clock or mutable random state.

The mutable medieval `FoundationWorld` envelope is now v2 and persists this state plus its own content-safety audit; `WorldManifest` v5 remains immutable creation provenance. `world.ts` validates the temporal provenance against the manifest and projects scheduler records into causal history, while storage rejects a save whose projection, safety audit, queue, or ordering does not reproduce. v1 medieval world envelopes (and older chronicles containing them) are rejected without migration because their zero-time field cannot prove the action sequence, pending queue, ordering, or dynamic policy audit; no prototype namespace is opened or changed. The renderer/browser path, BigBlue Terminal setup, palette, and gameplay systems remain untouched.

Verification for the completed action-clock and scheduler slice: on 2026-09-01, `npx vitest run src/medieval/temporal.test.ts src/medieval/world.test.ts src/medieval/storage.test.ts src/medieval/world-manifest.test.ts --maxWorkers=1 --no-file-parallelism` passed 38 tests in 4 files; `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 81 tests in 13 files; `npm run build` passed (Vite transformed 152 modules and the bundle-size check passed); and `git diff --check` passed. npm emitted the existing unsupported `email` configuration warning; no required final command failed. Playwright was not run because this renderer-independent slice changes no browser-visible behaviour.

- [x] Define versioned world state and persistence contracts for geography, sites, routes, markets, people, institutions, history, and Jomon.

Implementation design for the active versioned-world-state slice:

- `src/medieval/world-state.ts` will own mutable-world-state contract v1 and independently version its geography/frontier, sites/quays, routes/conditions, markets, people registry, institutions, history, Jomon, courier, and temporal subdomains. `types.ts` will keep the immutable foundation records and root envelope; `world.ts` will construct/reconstruct validated state from immutable provenance; and `storage.ts` will atomically persist only validated complete v3 world/ v2 chronicle envelopes in the existing medieval IndexedDB namespace.
- `WorldManifest` will advance to v6 and contain only immutable creation provenance. The mutable courier choice and current safety audit move into `MedievalWorldState`, alongside the temporal scheduler and causal-history projection. Initial-world data and frontier-root provenance remain immutable outer evidence; the mutable geography container retains only the bounded validated `FrontierState`, whose revealed/materialized facts are cross-checked against those roots.
- State records use stable existing IDs and sorted arrays, never maps or untyped extension bags: initial settlement/materialized-region sites reference an initial world or frontier region; routes reference sites and existing waterways; market placeholders reference sites; crew and initial person seeds enter a registry with explicit vessel/site residences; institutions reference sites; Jomon references its vessel and a site/quay location; history is the canonical foundation-plus-temporal causal projection. v1 placeholder commodity, capacity, and condition values are closed and deliberately minimal.
- Construction uses only validated manifest seed/configuration/initial world plus the deterministic frontier and temporal contracts. Validation is pure, bounded, canonical, fail-closed, and checks aggregate/subdomain versions, exact record shapes, lexical ordering, ID uniqueness, references, expected seeded records, temporal provenance, causal projection, and content-safety audits. Unknown versions, malformed/tampered values, and earlier medieval world/chronicle envelopes are rejected rather than migrated because their missing state cannot be exactly reconstructed; settings records remain independently compatible.
- Focused tests will cover canonical construction/order, reconstruction and atomic save/load equivalence, temporal persistence, cross-domain and duplicate/reference rejection, immutable-manifest separation, version/malformed rejection, settings survival, and medieval/prototype storage isolation. No UI, simulation, market, travel, map, or persistent-person behaviour is added.

Completed versioned-world-state decision (2026-09-01): `src/medieval/world-state.ts` owns aggregate mutable-state v1 and independently versioned geography/frontier, sites/quays, routes, markets, people, institutions, history, Jomon, and courier subdomains. It stores only bounded canonical arrays: initial-settlement and materialized-frontier sites; Jomon quays; seeded initial-route conditions; empty closed commodity placeholders; references to existing crew and initial person seeds; initial institutions; canonical foundation-plus-scheduler causal history; and Jomon’s moored location, integrity, and capacity values. No named ungenerated-frontier person becomes a mutable record, and no full person, market, travel, or simulation behaviour is implied.

`WorldManifest` is now immutable v6 creation evidence only. `FoundationWorld` v3 retains immutable Jomon, crew, and initial-world data, while courier selection, frontier facts/materialization, temporal scheduler state, mutable history, and the current content-safety audit live in `MedievalWorldState`; `WorldChronicle` is v2 because it embeds that world envelope. Construction and validation recompute seeded references, initial frontier roots, canonical causal projection, content-safety audits, and temporal provenance, reject unknown/malformed/substituted versions, unordered or duplicate IDs, broken references, and extension bags, and return no unsafe record to storage. v1/v2 medieval worlds, v1 chronicles, and v5 manifests are deliberately rejected without migration: their missing/mixed mutable-state boundary cannot be proven exactly equivalent. The IndexedDB database name and creation-settings record stay unchanged; world/catalog writes remain one transaction, load/save clone values, settings profiles survive, and no prototype namespace is opened.

Verification for the completed versioned-world-state slice: on 2026-09-01, `npx tsc --noEmit && npx vitest run src/medieval/world-state.test.ts src/medieval/world.test.ts src/medieval/world-manifest.test.ts src/medieval/temporal.test.ts src/medieval/storage.test.ts --maxWorkers=1 --no-file-parallelism` passed 43 tests in 5 files; `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 86 tests in 14 files; `npm run build` passed (Vite transformed 153 modules and the bundle-size check passed); and `git diff --check` passed. npm emitted the existing unsupported `email` configuration warning; no required command failed. Playwright was not run because the schema-driven property-path change preserves browser-visible behaviour.

- [x] Define persistent individual records for every instantiated person: family, work, needs, relationships, injury, possessions, birth, death, location, memory, and commitments.

Implementation design for the active persistent-person slice:

- `src/medieval/persistent-person.ts` will own person contract v1, its bounded record types, deterministic foundation-crew instantiation, structured content records, and pure validation. `world-state.ts` will advance its aggregate and people subdomain versions, own the reference context and state audit, while `world.ts` and `storage.ts` will construct and accept only the new v4 world/v3 chronicle envelopes. The immutable v6 manifest and initial-world data remain untouched.
- Only the six generated Jomon crew become records now, using their existing crew IDs, names, roles, histories, relationships, conversation values, and equipment plus person-specific named RNG streams derived from immutable seed/configuration provenance. Initial-world person seeds and anonymous or merely named frontier commitments remain sources for bounded known-uninstantiated references, never entries in the mutable registry; a later materialization slice owns any additional instantiation.
- Each person record has adult life chronology and living/dead status; an explicit Jomon/site/future-region location; role, closed skills, capacity, assignment, and availability; needs, health, recovery, bounded injuries; owned possessions; directed reciprocal household relationships; family links to either an instantiated person or a clearly typed known-uninstantiated reference; classified memories; and typed commitments. Dead records persist but must be unavailable, unassigned, absent from courier selection, and have no active commitment.
- Every list is bounded and canonically ID-sorted. Validation rejects malformed/unknown versions and shapes, duplicate IDs, missing or non-adult birth data, incompatible life/death states, invalid locations/references/reciprocity/ownership, uninstantiated seed masquerading, active dead work, unsafe or unclassified names/memories/possessions/family facts, and a changed immutable crew source. The manifest reconstructs the same initial six records, while later valid mutable records remain save data rather than creation provenance.
- Focused tests will cover deterministic crew construction/reconstruction, complete-field and chronology/life validation, locations/work/ownership/family/relationship ordering, dead-person restrictions, seed/frontier non-instantiation, safety/malformed rejection, and isolated settings/world save-load continuity. No clock progression, autonomous simulation, fidelity, travel, delegation outcomes, map, tavern, trade, or combat behaviour is added.

Completed persistent-person decision (2026-09-01): `src/medieval/persistent-person.ts` owns persistent-person contract v1 and bounded, canonical records for the only currently instantiated people: Jomon’s six deterministic foundation crew. Each record retains the immutable crew ID, name, adult status, conversation value, role, generated history, directed reciprocal relationships, and equipment, then adds deterministic provenance-derived adult birth data, Jomon/site/future-region location, closed skills and capacity, assignment/availability, needs, health/injury/recovery, owned possessions, typed family links, memories, and commitments. A family link can refer either to an instantiated record or to a bounded, source-checked `known-uninstantiated-person` from an initial person seed or named frontier commitment; it never allocates that person in the mutable registry.

Aggregate mutable state is now v2 and its people subdomain is v2. Its safety audit covers all classified persistent-person text and records; validation rejects unknown shapes/versions, noncanonical or duplicate IDs, missing crew records, changed immutable crew evidence, non-adult or impossible life/death chronology, invalid site/frontier references, invalid ownership/reciprocity, unsafe or unclassified content, and active work, courier selection, or active commitments for dead people. Courier selection also requires the corresponding living, available persistent record. `FoundationWorld` is v4 and `WorldChronicle` is v3; v3/v1 mutable-world/person envelopes are intentionally rejected without migration because their person state cannot be exactly reconstructed. The immutable v6 manifest, deterministic action clock, medieval-only IndexedDB namespace, and independently compatible settings profiles remain separate and unchanged.

Verification for the completed persistent-person slice: on 2026-09-01, `npx tsc --noEmit && npx vitest run src/medieval/persistent-person.test.ts src/medieval/world-state.test.ts src/medieval/storage.test.ts` passed 26 tests in 3 files. The required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 97 tests in 16 files; `npm run build` passed (Vite transformed 154 modules and the bundle-size check passed); and `git diff --check` passed. npm emitted the existing unsupported `email` configuration warning; no required command failed. Playwright was not run because this renderer-independent schema slice makes no browser-visible change.

- [x] Define deterministic fidelity tiers for loaded places, nearby people, recurring agents, and distant individual/settlement summaries without discarding individual state.

Implementation design for the active fidelity-planning slice:

- `src/medieval/fidelity.ts` will own a versioned, renderer-independent, pure plan contract. It consumes a validated `FoundationWorld`, the immutable resolved `simulationFidelity` value, the selected active courier, and a canonicalized set of explicitly loaded simulation locations; it neither enters `MedievalWorldState` nor writes storage because it is fully derivable from existing persisted data.
- The plan will explicitly retain loaded Jomon/location context and assign every persistent person by stable ID to `loaded`, `nearby`, `recurring`, `distant-individual-summary`, `deferred`, or historical-only status. Only living records may enter processing tiers; dead records remain ID-addressable historical entries. Initial person seeds, ungenerated commitments, and future-person IDs are never individual plan entries.
- `focused`, `balanced`, and `deep` use fixed bounded budgets for loaded places/people, nearby people, recurring agents, distant individual summaries, and distant settlement/institution summaries. Selection is deterministic: active courier first, then qualifying candidates by current assignment, direct relationship/family connection, memory/commitment evidence, current capacity, and lexical stable ID. An overflow record remains intact in state and is marked deferred, never flattened or fabricated.
- Each assigned tier has a non-mutating cadence descriptor for the next due-event catch-up task: loaded uses one-minute canonical windows, nearby five minutes, recurring 30, distant people 120, and distant settlement/institution summaries 240; the plan exposes the next integer in-world minute eligible from the current action-clock time. It schedules no event and advances nothing.
- Validation will fail closed for invalid/full-world provenance, mismatched or absent active courier, malformed/duplicate/over-budget loaded locations, invalid Jomon/site/materialized-region references, invalid person locations, or noncanonical derived data. The plan produces no free text; it validates the existing world safety audit rather than creating a content-policy bypass.
- Focused tests will prove repeatability and input-order independence; all three budgets and deterministic overflow; complete person-record preservation; loaded/nearby/recurring/distant placement; Jomon/courier/location checks; dead and uninstantiated-person exclusion; invalid reference rejection; and zero-time/no-mutation behaviour. No due-event execution, autonomous decision, distant outcome, persistence schema, browser UI, or gameplay code is added.

Completed deterministic fidelity-planning decision (2026-09-01): `src/medieval/fidelity.ts` owns fidelity-planning contract v1 as a pure, renderer-independent derivation over one validated active `FoundationWorld`, its immutable resolved `simulationFidelity`, the persisted active courier ID, and an explicit unordered set of additional loaded simulation locations. It persists neither a plan nor a cursor: Jomon and its moored site are always explicit loaded context, while extra locations must be valid sites or materialized frontier regions. Invalid, duplicate, or over-budget locations, invalid world/configuration/courier context, invalid courier location, and unsafe future clock arithmetic fail closed with canonical diagnostics.

Every persistent person remains in the returned canonical ID-sorted plan without changing the full record in world state. Living people are assigned in descending processing detail to `loaded`, `nearby`, `recurring`, `distant-individual-summary`, or `deferred`; dead records are `historical-only` and have no cadence. Initial person seeds, ungenerated commitments, and future-person IDs never appear as individual agents. Pressure selection always puts the active courier first, then orders candidates by current assignment, amount of direct relationship/family/memory/commitment evidence, latest in-world memory, current capacity, and stable ID. Places and institutions similarly receive loaded, bounded distant-summary, or deferred assignments by stable ID. The fixed budgets are focused: 2 loaded places, 2 loaded people, 1 nearby, 1 recurring, 1 distant person, 2 loaded institutions, 2 distant settlements, and 2 distant institutions; balanced: 4/3/2/2/4/4/4/4; deep: 8/4/3/3/8/8/8/8 in that same order.

The plan only expresses later scheduler eligibility: loaded entities use one-minute canonical windows; nearby records use 5-minute windows; recurring records 30 minutes; distant people 120 minutes; and distant settlements/institutions 240 minutes. It creates no event, advances no clock, mutates no world record, and generates no player-facing text. No persistence schema changes were necessary for its original plan shape; the later catch-up integrity correction versions its durable scheduling evidence separately.

Verification for the completed fidelity-planning slice: on 2026-09-01, the initial `npx vitest run src/medieval/fidelity.test.ts --maxWorkers=1 --no-file-parallelism` run failed only because its test incorrectly equated lexical output order with the documented selection-priority order; the assertion was corrected without changing the planner. `npx tsc --noEmit && npx vitest run src/medieval/fidelity.test.ts src/medieval/persistent-person.test.ts src/medieval/world-state.test.ts src/medieval/temporal.test.ts --maxWorkers=1 --no-file-parallelism` then passed 29 tests in 4 files. The required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 104 tests in 17 files; `npm run build` passed (Vite transformed 154 modules and the bundle-size check passed); and `git diff --check` passed. npm emitted the existing unsupported `email` configuration warning; no final required command failed. Playwright was not run because this slice makes no browser-visible change.

- [x] Define due-event catch-up scheduling for every fidelity tier on each time-bearing action so distant people, institutions, markets, and delegated work receive deterministic due-window provenance while the courier is elsewhere.

Completed due-event catch-up decision (2026-09-02, superseded where necessary by the corrective v3 contract below): `src/medieval/simulation-catchup.ts` owns bounded due-window scheduling/provenance. After the temporal reducer accepts a time-bearing action, `world.ts` derives the fidelity plan and records due targets without adding market, institution, relationship, need, or other domain-outcome rules. The later correction makes each tier’s cadence a canonical elapsed-time window rather than an action batch, preserving the same bounded no-wall-clock/no-mutable-RNG foundation.

The persisted simulation container has canonical per-target cursors, an inspectable bounded observation window, and typed delegated-work placeholders. The placeholder only tracks a known assignee’s cadence progress; it does not implement the Phase 1.4 offer, refusal, assignment, social, or UI model. Markets retain deliberately empty commodity state, and institutions retain their generated references: this layer records their scheduled processing only, without economy or institution gameplay. Every generated scheduling observation carries structured `simulation-summary` safety metadata and participates in the state audit.

The later corrective contract advances the mutable/state envelopes and rejects older action-batched evidence without migration because its canonical cadence windows cannot be reconstructed. The immutable v6 manifest, IndexedDB namespace, creation settings, browser route, and legacy prototype remain unchanged.

Verification for the completed catch-up slice: on 2026-09-02, `npx tsc --noEmit && npx vitest run src/medieval/simulation-catchup.test.ts src/medieval/world.test.ts src/medieval/world-state.test.ts src/medieval/fidelity.test.ts src/medieval/storage.test.ts --maxWorkers=1 --no-file-parallelism` passed 44 tests in 5 files. `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 111 tests in 18 files. `npm run build` passed (Vite transformed 156 modules and the bundle-size check passed); `git diff --check` passed before this documentation update. Playwright was not run because the renderer/browser path is unchanged.

- [x] Ensure detailed and summary simulation cannot emit prohibited content or encode it as an undiscoverable background cause.

Completed simulation-outcome safety/provenance decision (2026-09-02, superseded where necessary by the corrective v3 contract below): every scheduling observation and its closed `fidelity-cadence` cause is classified. A cause contains no free text: it names only a known person/market/institution record or typed delegated-work fact and its bounded cadence-window evidence. State validation cross-checks target, cadence window, detail level, due intervals, and a deterministic immutable-provenance token; both observation and cause enter the persisted structured content-safety audit. Missing, unsafe, noncanonical, untraceable, or token-forged data rejects before save/load without mutating valid state. The bounded observation window and cursor retain only validated scheduling facts, and delegated work remains a typed placeholder.

The corrective v3 contract advances `MedievalWorldState` to v5, `FoundationWorld` to v7, and `WorldChronicle` to v6. Earlier mutable envelopes are rejected without migration because they cannot prove the required canonical cadence-window cause/evidence path. The immutable `WorldManifest` remains v6; the medieval IndexedDB namespace, settings records, browser route, and prototype saves remain unchanged.

Verification for the completed simulation-outcome safety/provenance slice: on 2026-09-02, `npx tsc --noEmit` passed. `npx vitest run src/medieval/simulation-catchup.test.ts src/medieval/world-state.test.ts src/medieval/storage.test.ts src/medieval/fidelity.test.ts --maxWorkers=1 --no-file-parallelism` passed 37 tests in 4 files. `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 115 tests in 18 files. `npm run build` passed (Vite transformed 156 modules and the bundle-size check passed), and `git diff --check` passed before this documentation update. Playwright was not run because this renderer-independent schema contract makes no browser-visible change.

- [x] Correct catch-up provenance validation and time-partition invariance; define the current layer honestly as a deterministic scheduling/provenance kernel pending domain simulation.

Implementation design for the completed catch-up integrity correction:

- `world.ts` will expose one complete foundation-world validation path for storage and every mutable transition: reproducible manifest, immutable initial-world/Jomon/crew evidence, content-safety audits, mutable-state schema, temporal integrity, and catch-up provenance must all pass before a clone is advanced or finalized. Invalid input remains untouched.
- `fidelity.ts` and `simulation-catchup.ts` will replace action-batched global processing with canonical cadence windows. Each target’s stable token derives only from immutable world provenance, target identity, cadence, and the final crossed canonical window; cursors aggregate the bounded interval count rather than iterating per minute. Temporal action history may differ between equivalent partitions, but the exported canonical simulation projection must not.
- The persisted layer continues to record validated scheduling/provenance and delegated-work placeholder progress only. It does not claim to mutate needs, relationships, markets, or institutions: later domain simulations consume these canonical windows and cursors.
- Focused regressions will compare one long wait, unit waits, and mixed partitions; cover cadence crossings, all relevant fidelity tiers, delegated work, bounded long spans, malformed provenance/token/cause/safety data, and action-boundary rejection of tampered immutable evidence.

Completed catch-up integrity decision (2026-09-02): `validateFoundationWorld` is the single fail-closed foundation-world boundary now used by storage and every public mutable-world transition (`chooseInitialCourier`, `advanceFoundationWorldTime`, and chronicle finalisation). It verifies the reproducible manifest and immutable initial-world/Jomon/crew evidence, creation and mutable content-safety audits, versioned mutable state, temporal integrity, and the catch-up plan projection before a clone is returned. A rejected input is never mutated. The initial world, Jomon, immutable crew, persistent crew-person, and manifest-linked evidence each have in-memory action-boundary regression coverage.

`fidelity.ts` contract v2 exposes only canonical `time-window` cadences: loaded records use one-minute windows, then nearby/recurring/distant individual/distant place and institution windows use 5/30/120/240 minutes. Catch-up contract v3 derives a record ID and token from immutable world ID/creation digest, target ID/kind, cadence, and canonical final window—never an action ID, action count, or action timestamp. It folds all windows crossed by an action once per planned target, so its work is bounded by planned targets and retained observation capacity rather than elapsed minutes times population. The explicit `simulationCatchUpProjection` contains canonical cursors and delegated-work progress but omits the bounded action-observation history; equivalent total elapsed wait time therefore has the same projection for 5 versus 1+1+1+1+1 and 2+3, and for 240 versus 120+120 across all scheduled tiers. Temporal action history is intentionally allowed to differ.

This remains a deterministic, persisted, safety-audited scheduling/provenance kernel. It records due windows and delegated placeholder progress only; it does not change needs, relationships, markets, institutions, or other domain state. Later domain simulation consumes canonical windows/cursors rather than action batches. The v5 mutable state/v7 world/v6 chronicle envelope is a clean compatibility break: v6-and-earlier worlds or chronicles are rejected without migration because action-batched evidence cannot exactly reconstruct canonical window provenance. Manifest v6, medieval-only IndexedDB namespace, settings profiles, browser route, font, palette, and prototype isolation are unchanged.

Verification for the completed catch-up integrity correction: on 2026-09-02, `npx tsc --noEmit && npx vitest run src/medieval/world.test.ts src/medieval/fidelity.test.ts src/medieval/simulation-catchup.test.ts src/medieval/world-state.test.ts src/medieval/storage.test.ts --maxWorkers=1 --no-file-parallelism` passed 48 tests in 5 files. The required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 115 tests in 18 files. `npm run build` passed (Vite transformed 156 modules and the bundle-size check passed). `git diff --check` passed after this documentation update. npm emitted the existing unsupported `email` configuration warning; no required final command failed. Playwright was not run because no browser-visible behaviour changed.

- [x] Define a versioned world-era model for base, NG+, and NG++ states. Accumulated in-world active-play time and Jomon’s growth advance it; transition conditions, inspectable causes, persistence, and configuration hooks must be deterministic.

Implementation design for the active world-era slice:

- `src/medieval/world-era.ts` will own era contract v1: the closed `base`/`ng-plus`/`ng-plus-plus` states, integer progress calculation, compact durable-Jomon-growth evidence ledger, canonical transition records, validation, and derived forward-looking profiles. `world-state.ts` will version and persist the bounded era subdomain; `world.ts` will invoke its pure reducers only after a valid temporal action and canonical fidelity/catch-up work, while storage continues to admit only a fully validated world.
- Every accepted time-bearing action contributes its integer duration to `activePlayMinutes`; pure commands never reach the reducer. `eraPace` is immutable manifest provenance and maps `measured`/`brisk`/`pressing` to integer time multipliers 1/2/3. v1 combined-progress thresholds are 7,200 units for `base` → `ng-plus` and 21,600 for `ng-plus` → `ng-plus-plus`; durable evidence adds only centrally defined weights for closed physical-expansion, workspace-refit, capacity-upgrade, tool-installation, and small-craft kinds.
- Growth evidence has a canonical stable ID, closed kind, validated Jomon vessel/prop source reference, exact current in-world timestamp, and no caller-supplied weight or prose. The ledger is ID-sorted, duplicate-free, and capped at 32 entries. Transition records are recomputed from the active-minute total plus ordered evidence, retain only typed threshold evidence, exact crossing minute, model version, and an immutable-provenance-derived ID/token; they contain no free text and are capped at two v1 transitions.
- Validation recomputes paced time, growth units, total progress, era, transition history, profile, and NG++ cycle from immutable world ID/creation digest, resolved `eraPace`, temporal time, and canonical evidence. Older mutable/world/chronicle envelopes lacking that proof are rejected without migration; the immutable manifest, its `eraPace`, settings profiles, browser path, and prototype storage remain unchanged.
- NG++ never increases a raw escalation value. Its bounded semantic pressure/opportunity tags and remix identity are derived from immutable provenance plus the canonical 7,200-unit remix cycle for later route, scarcity, social-memory, frontier, relic, and content-family consumers; no such consumer or content is implemented here. Focused tests will cover pace/threshold boundaries, growth validation/order/duplicates, ordered intermediate transitions, plateau/remix profiles, action partition/reload equivalence, tamper rejection, and immutable/unrelated-state preservation.

Completed world-era decision (2026-09-02): `src/medieval/world-era.ts` owns world-era contract/model v1. It persists canonical `base`, `ng-plus`, or `ng-plus-plus` state; raw active-play minutes; derived paced time, durable-growth, and combined-progress units; a bounded canonical growth-evidence ledger; exact threshold transitions; and a derived profile/remix cycle. Every accepted time-bearing temporal action contributes its full integer duration; pure UI commands never reach the era reducer. Immutable resolved `eraPace` maps `measured`/`brisk`/`pressing` to 1/2/3 paced-time units per active minute. The centralized v1 thresholds are 7,200 combined units for base → NG+ and 21,600 for NG+ → NG++.

Only five closed durable Jomon evidence kinds exist: `physical-expansion` (2,400 units), `workspace-refit` (1,200), `capacity-upgrade` (1,800), `tool-installation` (900), and `small-craft` (3,600). Evidence contains a stable ID, one of those kinds, a validated immutable Jomon vessel/prop source, and the exact current in-world minute—never a caller-supplied weight or prose—and is ID-sorted, duplicate-free, and capped at 32. The zero-time `recordDurableJomonGrowth` world reducer validates the whole world then records only such completed durable work; it does not advance the clock. Repairs, cargo, temporary buffs, and ordinary arbitrary numbers have no evidence type.

Transition history is recomputed from active minutes plus chronologically applied evidence (same-minute ties by stable ID): time at a minute boundary applies before growth stamped at that minute. Each record names from/to era, exact crossing minute, model version, a typed combined-threshold cause with time/growth evidence, and a deterministic ID/token derived from immutable world ID/creation digest; no record contains prose. A multi-threshold action records ordered intermediate transitions exactly once. Because action IDs/counts are absent from the model, equivalent active minutes and equivalent evidence yield the same `worldEraProjection` regardless of action partitioning, while temporal action history may differ.

NG++ uses raw-escalation `plateau` permanently. Its 7,200-unit remix cycle and bounded closed pressure/opportunity tags are deterministically derived from immutable provenance, resolved pace, and cycle; they are non-authoritative hooks only for later route, scarcity, social-memory, frontier, relic, and content-family systems. This slice adds no events, content, enemies, numerical scaling, trade, travel, map, combat, social simulation, or era UI. Era data itself contains only closed semantic tags and no player-facing text; the mutable safety audit therefore continues to cover actual classified text-bearing records while the era validator rejects extension fields.

At completion, the era slice established `MedievalWorldState` v6, `FoundationWorld` v8, and `WorldChronicle` v7. The era state is fully recomputed and cross-validated against temporal provenance, the immutable manifest’s resolved `eraPace`, and Jomon vessel/prop identities on construction, action advancement, finalisation, local save/load, and reconstruction. `advanceFoundationWorldTime` validates the complete world, completes temporal/fidelity/canonical catch-up work, then commits the pure era result without mutating immutable provenance, crew identity, Jomon identity, or unrelated simulation state. The later causal-history slice upgrades those envelopes to v7/v9/v8 and rejects the previously valid pre-journal records without migration because they cannot prove a replay checkpoint/journal; manifest v6, settings profiles, the medieval IndexedDB namespace, browser path, and prototype storage remain unchanged.

Verification for the completed world-era slice: on 2026-09-02, `npx vitest run src/medieval/world-era.test.ts src/medieval/world-state.test.ts src/medieval/world.test.ts src/medieval/storage.test.ts --maxWorkers=1 --no-file-parallelism` passed 48 tests in 4 files. The required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 129 tests in 19 files. `npm run build` passed (Vite transformed 157 modules and the bundle-size check passed), and `git diff --check` passed after this documentation update. npm emitted the existing unsupported `email` configuration warning; no required command failed. Playwright was not run because this renderer-independent slice has no browser-visible behaviour.

- [x] Add event sourcing or an equivalent inspectable causal history so world changes can be explained, replayed, and persisted within bounded storage.

Implementation design for the active causal-history slice:

- `src/medieval/causal-history.ts` will own a versioned, renderer-independent command journal with exactly three typed command kinds: initial-courier selection, accepted time-bearing temporal action, and durable Jomon-growth evidence. It will store canonical sequence-ordered command inputs, provenance-derived IDs/tokens, a single typed replay checkpoint, and typed compacted-segment summaries; temporal records and catch-up observations remain their existing domain-local derived evidence.
- Mutable state will persist one causal-history subdomain and will be rebuilt through shared pure command reducers. The checkpoint projection contains only the presently command-owned courier, temporal, catch-up, and era state—not immutable creation data, recursive history, or UI presentation records. Full foundation validation will replay the retained tail from genesis or the checkpoint and compare that projection with the saved state before storage or any public mutable transition accepts it.
- v1 retains at most eight tail commands, exactly one checkpoint, and eight compacted summaries. When appending a ninth tail command, it advances the checkpoint to the resulting projection, clears that tail, and stores a canonical typed summary for the compacted contiguous sequence/time range and kind counts. At the summary bound, the two oldest contiguous summaries merge deterministically. No prose, event payload, hidden mutation, or unbounded history is retained outside those contracts.
- Public courier, time, and durable-growth transitions will first validate the complete world, run the existing pure temporal/catch-up/era reducers, then append exactly one journal command. Replaying uses those same pure reducers without public-validation recursion or a second append. Rejected inputs, pure UI commands, failed validation, and scheduler-derived work append nothing. The journal is safety-audited, fail-closed on malformed/reordered/duplicate/forged/mismatched records, and preserves the existing time-partition invariant for catch-up and era projections while allowing distinct player command journals.

Completed causal-history decision (2026-09-02): `src/medieval/causal-history.ts` owns causal-history contract v1. Its closed journal vocabulary is `initial-courier-selected`, `time-bearing-action`, and `durable-jomon-growth`; every entry holds only typed reducer input, canonical sequence, content-safety classification, and an ID/token derived from immutable world ID and creation digest plus canonical kind/payload. Creation history remains immutable provenance, while temporal causal records and catch-up observations remain domain-local derived evidence rather than competing global journals. Pure UI/inspection/idle/paused/closed-browser activity, rejected commands, failed validation, and derived scheduler work append no journal entry.

The v1 replay projection is the command-owned courier, temporal state, canonical catch-up cursors/delegated-work state, and era state. The full foundation boundary validates its immutable world and mutable subdomains, replays the retained journal tail through the same non-public courier/time/growth reducers, and rejects any replay mismatch before a transition, save, load, reconstruction, or finalization proceeds. A sequence-zero checkpoint must equal immutable creation genesis; a compacted checkpoint is structurally/token/provenance validated and replays its remaining tail without claiming cryptographic tamper-proofing.

History is bounded to one checkpoint, eight retained commands, and eight typed compacted segment summaries. Appending the ninth retained command checkpoints that exact resulting projection, clears the tail, and records a canonical contiguous sequence/time range, kind counts, creation digest, and result-state digest. When eight summaries already exist, the two oldest contiguous summaries merge deterministically. Checkpoints and summaries contain no prose or recursive journal data. `MedievalWorldState` is v7, `FoundationWorld` v9, and `WorldChronicle` v8; pre-journal world v8-and-earlier and chronicle v7-and-earlier envelopes reject without migration because they cannot prove a replay checkpoint/journal. The immutable manifest, settings profiles, medieval IndexedDB namespace, font/palette/browser behavior, and prototype boundary are unchanged.

This remains a scheduling/provenance foundation: it does not add domain simulation, social AI, trade, maps, generated events, content families, or a causal journal for future command families. Later domain reducers must add their own closed journal command kinds and replay projections before becoming mutable world authority.

Verification for the completed causal-history slice: on 2026-09-02, `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 136 tests in 20 files. `npm run build` passed (TypeScript, Vite, and the bundle-size check), and `git diff --check` passed. npm emitted the existing unsupported `email` configuration warning; no required command failed. Playwright was not run because this renderer-independent slice has no browser-visible behaviour.

- [x] Add focused tests for reload equivalence, simulation determinism, due-event catch-up, prohibited-content rejection in detailed and summary ticks, zero-time UI/idle/paused/closed sessions, corruption recovery, and bounded state growth.

Completed verification decision (2026-09-02): the new cross-contract regressions treat the present layer honestly as a deterministic scheduling/provenance foundation, not people/market/institution gameplay simulation. Two distinct seed/configuration specimens now create a courier-selected world, resolve pending temporal events, record typed durable Jomon growth through an era threshold, compact the command journal, save/load, replay the checkpoint/tail, and continue with one valid command. Equivalent command sequences reproduce relevant medieval state exactly; different wait partitions intentionally retain different temporal/journal evidence while their canonical catch-up and era projections remain equal. Coverage includes all currently scheduled fidelity/cadence families (loaded, nearby, recurring, distant individual, loaded/distant settlement, loaded/distant institution, and delegated-work placeholders), canonical same-minute priority/ID event ordering, and a bounded long-duration cadence case.

The safety regression suite exercises both represented scheduler output forms: detailed loaded/nearby/recurring records and summary distant person/place/institution records. Prohibited or missing structured classifications on either an outcome or its cause now demonstrably reject at full-world validation, public transition, repository save, and repository load boundaries. Pure inspect/settings-opened/settings-closed/route-changed/browser-idle/browser-paused/browser-reloaded/generic-UI temporal commands reject without changing clock, catch-up, era, journal, or mutable world state. The Chromium keyboard flow now creates and saves a real world, idles, opens/closes settings, reloads/resumes, closes its page, and resumes from a replacement page in the same context; direct IndexedDB inspection confirms zero world minutes, zero action sequence, and only the accepted courier-selection journal command.

Current corruption-containment contract: malformed, forged, incompatible, or replay-mismatched medieval worlds, chronicles, catalog indices, and causal checkpoint/tail records are contained and rejected as unavailable/default index data without best-effort repair, migration, or deletion. Reads preserve corrupt IndexedDB entries; valid independent worlds and settings remain loadable, and an explicitly saved valid replacement remains resumable. This is not snapshot, export, or automatic recovery, which remains deferred to the later storage task. Bounded tests cover temporal queue/causal-record refusal, canonical scheduler-observation eviction and delegated-placeholder refusal, non-evicted bounded era evidence/transitions, and causal tail/checkpoint/compacted-summary compaction. There is no standalone mutable `history` array: the bounded causal checkpoint/tail/segments are the current authoritative state-history container.

No production behavior changed for this verification slice. Verification on 2026-09-02: `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 145 tests in 21 files. `npx playwright test e2e/medieval-foundation.spec.ts --project=chromium` passed 2 Chromium tests. `npm run build` passed TypeScript, Vite (158 modules), and the bundle-size check. `git diff --check` passed after this documentation update. npm emitted the existing unsupported `email` configuration warning; Playwright also emitted the environment's existing `NO_COLOR`/`FORCE_COLOR` warning. No product defect was found; two transient test-authoring issues (an IndexedDB helper argument and a callback-array annotation) were corrected before the final passing runs.

Acceptance: equivalent active-play time produces the same world state and era across replay and reload; the world never advances while inactive; a visible change or escalation has an inspectable causal record.

#### 1.4 People, conversation, and delegated work foundation

- [x] Define persistent-person state: identity, household/site, role, material interests, skills, relationships, memories, current work, capacity, health, and commitments.

Completed persistent-person decision (2026-09-02): `src/medieval/persistent-person.ts` now owns persistent-person contract v2, extending rather than duplicating the v1 foundation-crew model. Every current instantiated person is an adult-only `foundation-crew` record with a stable crew-derived ID and preserves the seeded name, conversation value, crew role, history, reciprocal relationships, and equipment meaning. Initial-world person seeds and named/unrevealed frontier commitments remain non-instantiated; a family link may identify a validated known-uninstantiated adult source without creating a mutable person. Future person mutation/materialization must first define a closed source-linked origin and causal-history command/replay projection.

V2 separates durable household affiliation, typed home, and current location. Household anchors use the closed `jomon-household`/`site-household`/`frontier-household` vocabulary and locations can only identify Jomon, a validated site, or a validated frontier commitment of the matching kind. Current foundation crew have Jomon household, home, and location; site/frontier forms and closed local occupations are source-ready contract shapes only, not a bulk-instantiation path. Work now has a discriminated Jomon-crew-role/local-occupation representation, bounded typed skills/capacity/availability, and an `idle` or commitment-linked `committed` current-work state. Current work may name only the holder's active typed commitment; it does not predefine delegation assignments, outcomes, or mutable task prose.

Material interests are a closed, canonical, duplicate-free vocabulary of waterway knowledge, route safety, trade ledgers, goods care, craft work, vessel upkeep, household upkeep/security, provisions, care work, and records. Foundation crew receive a deterministic role-derived sorted set of at most three interests. Validation cross-checks household/home/location references, crew role correspondence, capacities/availability/current work, active commitments, adult birth/death/life status, injuries/recovery, possession ownership, reciprocal relationships, bounded family/memory data, and content-safety classifications. Dead people remain historical records and cannot be active couriers, available/committed workers, or hold active commitments.

The causal replay projection deliberately still excludes people because current records have no public mutation command; replay/checkpoint compaction preserves the validated registry unchanged. Behaviour, conversation effects, delegation, autonomous decisions, needs decay, trade, combat, maps, and player-facing UI remain unimplemented. The compatibility boundary is fail-closed without migration: persistent-person v1, `WorldPeopleState` v2, `MedievalWorldState` v7, `FoundationWorld` v9, and `WorldChronicle` v8-or-earlier records cannot prove the v2 record and are rejected. The current enclosing versions are persistent-person v2, people subdomain v3, mutable state v8, world v10, and chronicle v9; immutable manifest and settings contracts remain unchanged.

Verification for the completed persistent-person slice: on 2026-09-02, `npx vitest run src/medieval/persistent-person.test.ts src/medieval/storage.test.ts --maxWorkers=1 --no-file-parallelism` passed 34 tests in 2 files, and the required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 149 tests in 21 files. `npm run build` passed TypeScript, Vite (158 modules), and the bundle-size check; `git diff --check` passed. npm emitted the existing unsupported `email` configuration warning; no required command failed. Playwright was not run because this renderer-independent slice has no browser-visible behaviour.

- [x] Define the active courier’s `conversation` stat and how it changes delegation eligibility, negotiation, task clarity, trust, risk, and outcome without becoming universal or mind-controlling persuasion.

Completed conversation decision (2026-09-02): `src/medieval/conversation.ts` owns the renderer-independent v1 assessment contract. It accepts only a fully valid `FoundationWorld` and a v1 closed proposal: `request`, `coordination`, or `terms`; `routine` or `pressing` urgency; `routine` or `involved` complexity; one existing closed material-interest tag; and a valid content-safety classification. The active courier's existing persistent `identity.conversation` (the authoritative seeded integer 1–5) is the only conversation stat: 1 unlocks `direct-request` with limited clarity/minimal support; 2 adds `shared-context` with basic/limited; 3 adds `terms-outline` with clear/clear; 4 adds `contingency-check` with precise/strong; and 5 adds `reciprocal-options` with thorough/expert. It has no stat growth or second persuasion/trust value.

An assessment is pure, zero-time, and has no storage, journal, scheduler, catch-up, era, person, relationship, memory, commitment, or UI mutation. It reports only canonical closed factor codes, bands, and a content-safety classification—never prose, percentages, or an accept/refuse/success outcome. It requires a living selected courier and a distinct living instantiated recipient at the same valid location. Missing/non-active/dead/unavailable couriers; missing, self, dead, absent, unavailable, zero-capacity, currently committed, actively committed, urgent-needs/safety, and strained/injured recipients are explicit hard barriers. Recipient-directed relationship standing, the recipient's closed material interests, capacity, work/commitments, needs, health/recovery, safety pressure, and proposal urgency/complexity remain authoritative over rapport, risk, and readiness; maximum conversation cannot bypass them. Agreement/refusal, task creation/assignment/progress/outcomes, memories, dialogue, and UI remain future work.

No persisted field or schema changed: this is a v1 pure contract over the current persistent-person v2 / people v3 / mutable world v8 / `FoundationWorld` v10 contracts. Future person-mutating or delegation authority must add a closed causal-history command and replay-projection support before mutation. Verification on 2026-09-02: `npx vitest run src/medieval/conversation.test.ts --maxWorkers=1 --no-file-parallelism` passed 6 tests in 1 file; `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 155 tests in 22 files; `npm run build` passed TypeScript, Vite (158 modules), and the bundle-size check; `git diff --check` passed after this documentation update. npm emitted the existing unsupported `email` configuration warning. Playwright was not run because this slice has no browser-visible behaviour.

- [x] Define a constrained task/delegation model for crew and NPCs: offer, agreement/refusal, assignment, progress, interruption, outcome, and later memory.

Completed delegation decision (2026-09-02): `src/medieval/delegation.ts` owns delegation contract/state v1. Its closed, canonical table covers maintenance, rigging, cooking, treatment, cargo handling, trade research, barter, bookkeeping, scouting, charting, gathering, hunting, guiding, watch duty, guarding, rescue, evacuation, recruitment, correspondence, witness work, and negotiation. Each definition is limited to relevant closed interests/skills, routine/involved complexity, routine/pressing urgency, fixed integer elapsed-minute duration, risk band, and explicitly non-authoritative future-domain hooks. The table produces no vessel, cargo, market, health, frontier, recruitment, combat, or social effects.

An offer names the selected living courier, a distinct instantiated living adult recipient, an approved family/proposal, and an approach already unlocked by the v1 `assessCourierConversation` gate. The public transition validates the complete foundation world, performs a one-minute `delegated-task-commitment` action, then produces a typed agreement/refusal from the assessment, recipient-directed relationship, interests, relevant skill/capacity, needs, health, active work/commitments, task risk/complexity, and the named immutable-world/digest/task/boundary seeded stream. It records only closed factors, roll/threshold bands, and deterministic tokens—never caller-supplied chance, prose reasons, or an accept/success claim. Hard conversation blockers and recipient authority remain decisive; conversation cannot force acceptance.

Accepted work reserves one capacity unit, creates exactly one typed `delegated-task` commitment/current-work link and scheduler placeholder, and has a fixed canonical due minute. Refusal is terminal without active work. Every later time-bearing action folds task progress; all crossed tasks complete at their own due minute in due-time/ID order, independent of action partitioning and even when the courier is elsewhere. V1 interruption is also a one-minute action and only permits `courier-recall` or `courier-cancellation`; completion/release and interruption/cancellation restore the same capacity/work/commitment link. Outcomes currently create only validated task state and person-history evidence, not physical world effects.

The registry holds at most 24 ID-sorted records, 6 active tasks, and 18 terminal records; saturation rejects new work without discarding history. Courier and recipient retain immutable foundation history plus bounded source-linked social-memory references, keeping the newest by time then ID under canonical eviction. All task, proposal, assessment, factor, interruption, outcome, commitment, and memory records are classified and audit-validated; malformed, unclassified, unsafe, duplicate, impossible-lifecycle, uninstantiated, unavailable, dead, and cross-domain-inconsistent data fail closed. Initial-world person seeds and frontier commitments stay non-instantiated.

`causal-history.ts` v2 adds only `delegation-offered` and `delegation-interrupted` command kinds. Its replay/checkpoint projection now contains people v4 and delegation v1 because these commands mutate person work, commitments, and memories; generic time commands derive progress/completion without a second player command. `simulation-catchup.ts` reconciles deterministic cursors when an accepted/completed/interrupted task changes current fidelity eligibility at the same world minute, without adding time or an observation. The no-migration compatibility boundary is persistent-person v3, people subdomain v4, delegation v1, causal history/replay/checkpoint/segment v2, mutable world state v9, `FoundationWorld` v11, and `WorldChronicle` v10; prior medieval envelopes reject because they cannot prove the complete task/person replay projection. Immutable manifest/settings and prototype persistence remain unchanged.

Verification on 2026-09-02: `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 167 tests in 23 files; `npm run build` passed TypeScript, Vite (160 modules), and the bundle-size check; `git diff --check` passed. Focused delegation/persistence coverage includes all family definitions, acceptance/refusal, deterministic tokens, exact due completion, interruption/release, partition invariance, source-linked memory retention, safety rejection, canonical registry bounds, journal replay/checkpoint compaction, reload/continuation, pure-command non-mutation, and no extra person instantiation. npm emitted the pre-existing unsupported `email` configuration warning. Playwright was not run because this renderer-independent slice has no browser-visible behaviour. Autonomous behaviour, dialogue, task UI, economics, travel, map actions, combat, needs decay, relationship/memory simulation, and new content remain unimplemented.

- [x] Make autonomous choices arise from original needs, opportunities, relationships, and local conditions. Distant people use deterministic summary simulation; nearby and recurring people use richer state and behaviour.

Completed autonomy decision (2026-09-02): `src/medieval/autonomy.ts` owns autonomy contract/state v1. Its need values are bounded integers: `0` is settled and `5` is urgent. Every accepted time-bearing command folds crossed 30-minute canonical windows only. For each living instantiated person, a provenance-derived rotating need channel receives the fixed-window increment; quotient/remainder counting plus saturating arithmetic makes the fold independent of action count and avoids an elapsed-minutes × population loop. Initial-world seeds and frontier commitments remain non-people and never receive autonomy state.

The closed opportunities/choices are accepted delegated work/continue, recovery/seek recovery, urgent personal needs/address, household readiness/maintain, household support/support, and no active opportunity/hold-wait. Selection precedence is exactly active delegated task, recovering or injured health, any need at least `4`, at-home Jomon household readiness interests, at-home Jomon household support with positive recipient-directed courier relationship, then hold. All explanation is an ID-sorted closed factor-code set covering tier/detail, choice/opportunity, health, needs, capacity/work/task, household/location, courier relationship, role, and era. There is no prose rationale, mutable social-memory effect, or autonomous task creation; delegation completion and interruption remain delegation-owned.

Current fidelity determines evidence detail only: `loaded`, `nearby`, and `recurring` receive detailed individual observations; `distant-individual-summary` receives a compact summary; deferred and historical-only people, unavailable people, zero-capacity people, dead people, and all uninstantiated commitments receive no active observation. The persisted subdomain has ID-sorted per-living-person processing cursors (maximum 24), one replace-in-place latest observation per schedulable person (maximum 24), and at most 16 canonical factor codes per observation. Every observation is safety-classified and its token is derived from immutable world provenance, person ID, tier, and canonical catch-up boundary; invalid IDs, tiers, cursors, ordering, token, factor, bounds, or content audit reject fail closed.

The canonical reducer order is temporal acceptance, era accounting, catch-up folding, delegation lifecycle/placeholder resolution, fidelity/cursor reconciliation, then autonomy folding and observation projection; the public command is appended only after that result exists. Generic time commands therefore replay the same autonomy result without a new journal command. Causal-history replay/checkpoints now carry autonomy, while public fidelity planning keeps its full validation boundary and internal already-validated reducer/replay calls use the same planner computation without repeatedly regenerating immutable foundation evidence. This fixes the replay-cost regression without bypassing public full-world validation.

Compatibility is fail-closed and has no migration: autonomy contract/state and autonomy world subdomain are v1; causal history/replay/checkpoint/segment are v3; mutable world state is v10; `FoundationWorld` is v12; and `WorldChronicle` is v11. Earlier medieval mutable envelopes cannot prove folded need or autonomy replay state and reject. Immutable manifests, settings profiles, renderer/font/palette behavior, and prototype persistence remain unchanged. This slice advances only person need pressure and autonomy evidence. It does not perform economics, physical work effects, healing, consumption, relationship or memory mutation, combat, travel, social AI, new content, or frontier materialization.

Verification on 2026-09-02: `npx vitest run src/medieval/storage.test.ts src/medieval/delegation.test.ts src/medieval/causal-history.test.ts src/medieval/foundation-hardening.test.ts --maxWorkers=1 --no-file-parallelism` passed 48 tests in 4 files after the replay-cost correction. The required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 173 tests in 24 files. `npm run build` passed TypeScript, Vite (161 modules), and the bundle-size check; `git diff --check` passed before this documentation update. npm emitted the existing unsupported `email` configuration warning. Playwright was not run because this renderer-independent slice has no browser-visible behaviour.

- [x] Define the compact, collapsible management sidebar that is visible by default and provides comprehensive household-known task, people, work, risk, site, route, and history information without displacing the primary map. Label source, discovery time, and freshness; do not expose hidden global state. Assign complementary, non-duplicative roles to map marks, physical notices, messages, ledgers, and tavern conversations.

Implementation design for the active management-sidebar slice:

- `src/medieval/management-sidebar.ts` will own a versioned, renderer-independent, read-only projection. It accepts only a fully validated `FoundationWorld`, produces six fixed ID-sorted and bounded sections—overview, people/work, tasks, risks, known sites/routes, and history—and attaches each fact’s stable ID, category, closed source type/label, recorded/discovery minute, freshness (`current`, `timeless`, or an explicit reported-freshness minute), structured value, and content-safety classification. The canvas is an adapter only and has no sidebar persistence or world mutation.
- The knowledge allowlist is deliberately narrow: Jomon/active courier/current world-time/era state; instantiated household people and their work/needs/health/autonomy evidence; the household’s delegated tasks; risk facts directly implied by those records; journal tail/compacted accounting; and frontier facts only when a non-ungenerated region already carries a valid revealed fact and its source/timing. The model will not inspect initial-world names/counts/routes/sites/people/institutions/markets, ungenerated commitments, anonymous templates, future names, or global route conditions merely because they exist in the save.
- Fact kind, source, priority, and freshness vocabularies will be closed and no `unknown` value bag will cross the projection boundary. The generator will canonicalize essential/urgent facts before standard facts, audit every displayed text-bearing source classification, reject malformed or unsafe worlds fail closed, and leave the supplied world unchanged. The module will define the complementary surface contract: sidebar is strategic household-known summary; future map marks are spatial/navigation only; physical notices/props are situated evidence/actions; messages announce arrivals/urgency; ledgers carry fuller durable records/contracts; tavern conversations carry person-specific consent, negotiation, and perspective.
- `app.ts` will reserve an independent main/map-reserved left panel and a right management panel inside the current 800×630 canvas. Sidebar state is pure UI state: expanded by default, `M` toggles it, `[`/`]` cycle its six sections when expanded, and `Esc` keeps its existing world-return meaning. It will use existing semantic palette tokens, BigBlueTerm, measured per-panel wrapping, data attributes, and one concise aria summary. No map, direct task control, browser timer, or save mutation is introduced.
- Focused projection tests will cover canonical bounds/metadata, allowlisted people/work/tasks/risks/history, source-gated frontier facts, deliberate omission of undiscovered generated state, safety/malformed rejection, and zero mutation. Chromium coverage will exercise keyboard creation/courier selection/sidebar controls and IndexedDB confirmation that sidebar UI leaves time, task, autonomy, era, and causal state unchanged; a temporary desktop screenshot will be inspected without committing an artefact.

Completed management-sidebar decision (2026-09-02): `src/medieval/management-sidebar.ts` owns the v1 renderer-independent, read-only projection. Its six fixed sections are `overview`, `people-work`, `tasks`, `risks`, `known-sites-routes`, and `history`; each is canonical priority/time/ID ordered and holds at most six facts (overview holds four). Every fact has a stable `management:` ID, closed category/priority/source type/source label, recorded/known-at minute, `current`/`timeless`/reported-freshness state, structured value, and a separately audit-validated content-safety classification. The projection has no persisted state and does not mutate its validated `FoundationWorld`, so no schema or compatibility version changes are required.

The household-knowledge allowlist is Jomon, active courier and clock/era state; the instantiated crew’s own work/needs/health/autonomy evidence; household-created delegated tasks and their direct risks; causal-journal tail/compacted accounting; and only already revealed non-ungenerated frontier facts with their existing source/time evidence. It intentionally omits initial-world generated names/counts/routes/sites/people/institutions/markets, ungenerated commitments, anonymous templates, and future names. Sidebar is the compact strategic household-known summary; future map marks own spatial navigation, physical notices/props own situated evidence/actions, messages own newly arrived/urgent change, ledgers own fuller durable records/contracts, and tavern conversations own person-specific consent, negotiation, and perspective. None is an omniscient substitute for another surface.

`app.ts` renders an expanded-by-default right panel and a separately bounded left map-reserved panel in the existing 800×630 BigBlueTerm canvas. `M` toggles management; `[`/`]` select the six sections while expanded; `Esc` retains its world-return behaviour. Both panel widths use measured bounded wrapping; the visible compact fact metadata is `SRC <closed source> K<known minute> <freshness>`, with text/glyph priority cues alongside palette colours. The canvas exposes `data-management-visibility`, selected section, current-section item count, known/urgent counts, and a concise aria summary. Sidebar input is read-only: it creates no clock, task, autonomy, era, journal, or storage mutation. No map, direct action, dialogue, trade, travel, combat, autonomy rule, or detailed renderer was added.

Verification on 2026-09-02: `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 178 tests in 25 files (68.87s). `npx playwright test e2e/medieval-foundation.spec.ts --project=chromium` passed 2 tests (4.1s): real keyboard creation/courier selection, default expansion, section cycling, collapse/expand, aria/data attributes, IndexedDB zero-time/no-mutation checks, reload, replacement-page persistence, and no external request. A fresh 1280×800 Chromium screenshot was manually inspected: left/right panel borders, wrapping, contrast, `*`/`-` state cues, and `SRC`/known/freshness cues stayed within the canvas; no screenshot artefact was added to the repository. `npm run build` passed TypeScript, Vite (162 modules), and the bundle-size check. npm emitted the pre-existing unsupported `email` configuration warning; Playwright also emitted the existing `NO_COLOR`/`FORCE_COLOR` warning.

- [x] Add original social-memory records and player-readable evidence through physical surfaces, messages, conversations, ledgers, rumours, goods, routes, visible work, and the management sidebar. Do not reproduce proprietary named-system hierarchies or vendettas.

Implementation design (2026-09-02): `src/medieval/social-memory.ts` will own a v1 bounded social-record registry for only existing delegation outcomes: accepted offers, refusals, canonical due-time completions, and courier interruptions. Each record will be derived from its validated task and direct command or canonical due-time source, with sorted real adult participant IDs, exact world minute, closed phase/disposition/significance/factor codes, a provenance-derived token, and an event-domain safety classification. It will replace derived `task-evidence` prose with typed, source-linked personal `social-memory` references; immutable foundation memories remain untouched. At most 48 social records and the mutable portion of each person’s 12-memory cap are retained. Personal links retain most-recent minute/ID records deterministically; a still-retained social record with no individual link becomes an explicit household-record-only entry rather than an orphan or hidden memory.

World state and causal replay checkpoints will carry this social registry, and the shared delegation/time reducers will derive it without creating a second journal command. Validation will reconstruct expected task-linked social records, cross-check participant links/tokens/order/safety/retention, and reject malformed, unsafe, reordered, orphaned, or replay-mismatched state. The conversation contract may consume only a pure, retained pair-recall band, subordinate to all existing recipient authority and hard blockers. The only rendered evidence is a source/known-at/freshness-labelled sidebar history fact. Typed future routes are `tavern-conversation`, `ledger`, `message`, `physical-notice`, `rumour`, `goods-mark`, `route-evidence`, and `visible-work`; none is rendered or treated as global knowledge here. This is not a hierarchy, vendetta, reputation score, dialogue, map, notice, message, rumour-spread, trade, travel, or physical-effect system.

Completed social-memory decision (2026-09-02): `social-memory.ts` v1 is the sole mutable shared-work recall registry. It produces only `offer-accepted`, `offer-refused`, `task-completed`, and `task-interrupted` records from existing delegation inputs and canonical completion minutes. Every record has a real task/offer source, two sorted instantiated living adult participants, exact occurred/known-at minute, closed disposition (`cooperative`/`declined`/`interrupted`), significance (`routine`/`notable`), factor codes, provenance-derived token, safety classification, and explicit `participant-retained` or `household-record-only` retention. It holds at most 48 records. Courier and recipient get typed source-linked social-memory references; immutable foundation history is never evicted. The mutable personal portion keeps at most 11 links by newest minute then ID, and the registry remains as a household-only accounted record when all participant links age out.

The pure pair-recall projection has only `none`, `supportive`, and `unsettled` bands. Conversation v2 exposes it as a canonical recipient-context factor and may shift its readiness band one bounded step, but never bypasses location, life status, availability, capacity, commitments, needs, health, interests, existing relationship state, or refusal/outcome rules. It creates no trust/reputation score, relationship mutation, hierarchy, vendetta, coercion, dialogue, or person instantiation. Only `management-sidebar` is rendered now: social facts are source-labelled `SHARED MEMORY`, known-at, timeless records in the bounded history section. `tavern-conversation`, `ledger`, `message`, `physical-notice`, `rumour`, `goods-mark`, `route-evidence`, and `visible-work` are closed typed future routes only, each requiring a real social source record; no dialogue, map, physical-message, goods, route, or rumour UI exists.

Persistent-person v4 replaces `task-evidence` prose with discriminated social-memory links. Delegation contract/state v2 and conversation v2 preserve the expanded assessment and source-linked task memory boundary. Mutable world state is v11 (`people` v5, `delegation` v2, `socialMemory` v1); causal-history/replay/checkpoint/segment contracts are v4; foundation worlds are v13 and chronicles v12. The manifest and local settings remain unchanged. Older mutable medieval records are rejected without migration because they cannot prove the new social source/replay projection; prototype storage remains unopened. Shared delegation/time reducers derive social state during normal execution and replay, while causal commands remain only courier selection, time actions, durable growth, delegation offer, and interruption.

Verification on 2026-09-02: `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 184 tests in 26 files (66.93s), including accepted/refused/completed/interrupted source records, participant links, recall/context, compaction, safety/tamper rejection, sidebar facts, replay/checkpoint, storage reload, and no extra people. `npm run build` passed TypeScript, Vite (163 modules), and the bundle-size check. `npx playwright test e2e/medieval-foundation.spec.ts --project=chromium` passed 2 tests (3.9s). A fresh 1280×800 Chromium screenshot of a created world was manually inspected: the management panel boundary, wrapping, BigBlueTerm contrast, and textual/glyph cues remained readable and inside the canvas; no screenshot artifact was added. `git diff --check` passed. npm emitted the pre-existing unsupported `email` configuration warning; Playwright emitted the existing `NO_COLOR`/`FORCE_COLOR` warning.

- [x] Add deterministic tests for delegation, refusal, task interruption, memory, recurrence, and no-player-control autonomy.

Completed delegation/autonomy hardening decision (2026-09-02): cross-contract coverage uses public medieval world APIs and outcome discovery rather than reproducing private delegation random-stream formulas. It proves identical seed/courier/offer/interruption/action inputs reproduce validated and replayable accepted work; a single completion action and equivalent split waits retain the same task, person work/commitment, social-memory, autonomy, canonical catch-up, and era projections while their temporal/journal action records may differ. Refusal, canonical-due completion, and courier interruption each remain source-linked through task state, scheduler placeholders, social records, and the closed causal command vocabulary. A recall attempted from one minute before a due task has no partially returned mutation: the resolution action first completes at its canonical due minute, the interruption rejects, and no interrupted social record is added.

Repeated real material-interest-mismatch refusals use the public conversation/offer path, keep source IDs/times canonical, trigger the bounded causal checkpoint at the eighth retained command, survive local IndexedDB reload, and continue with the same pair recall. The existing `social-memory.test.ts` direct reducer coverage remains the focused proof of personal-link eviction beyond the 11-link cap: it preserves immutable foundation memories and retains aged records as explicit household-only entries. New cross-contract coverage proves a completed shared result yields supportive recall; a later refusal yields unsettled recall, appears as the sole bounded `recipient-recall:*` context factor, and never mutates relationship standing or forces agreement. A no-recall → unsettled comparison confirms the documented one-step readiness limit; switching between supportive and unsettled may span two bands because each distinct recall band is independently capped by that one-step rule.

Autonomy remains derived only from accepted time-bearing commands and the existing fidelity/catch-up plan: an active assignee observes `continue-accepted-delegated-work`, then returns to the existing normal bounded priority order after completion. Pure assessment and sidebar reads mutate neither world nor journal. No autonomy command is appended, and time processing does not create tasks, offers, commitments other than the authoritative delegation commitment, social records beyond actual delegation sources, people, relationships, locations, physical effects, or arbitrary narrative outcomes. Forged autonomy, task, and social-memory state rejects at full-world/action and medieval IndexedDB save/load boundaries while an independent valid world remains loadable. No production code or schema changed: social-memory v1, persistent people v4, conversation/delegation v2, mutable world v11, causal history v4, `FoundationWorld` v13, and chronicle v12 remain the clean compatibility boundary.

Verification on 2026-09-02: an initial focused draft exposed only test-fixture issues—a broad public-offer search exceeded Vitest’s default timeout, and a supportive-to-unsettled comparison incorrectly treated two independent one-step recall adjustments as one. The tests were narrowed to bounded public discovery and a no-recall comparison; no production defect was found. A first combined run then exceeded Vitest’s five-second default for full-world replay/storage checks, so the two fixed public lifecycle cases now use explicit 15–20 second test limits; they remain deterministic and bounded. Final `npx vitest run src/medieval/delegation-social-hardening.test.ts src/medieval/storage.test.ts --maxWorkers=1 --no-file-parallelism` passed 34 tests in 2 files (41.55s). Required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 190 tests in 27 files (102.23s). `npm run build` passed TypeScript, Vite (163 modules), and the bundle-size check; `git diff --check` passed. Playwright was not run because only unit tests and this roadmap changed, with no browser-visible production code. npm emitted the existing unsupported `email` configuration warning; no final required check failed.

Acceptance: deterministic cross-contract regressions prove the implemented delegation/social-memory/autonomy foundations remain replayable, bounded, non-omniscient, and free of player control over autonomous choices; they do not claim dialogue, task UI, social AI, or physical domain effects.

#### 1.5 Terminal presentation and interaction foundation

- [x] Define a renderer-independent map, glyph, palette, status, message, prompt, input, and accessibility contract.

Completed terminal-presentation decision (2026-09-02): `src/medieval/terminal-presentation.ts` owns terminal-presentation contract v3 as a pure, renderer-independent projection over a fully validated `FoundationWorld`. Its current map is deliberately a 64×24 `reserved-unmaterialized` viewport with zero cells: it exposes no terrain, deck, actor, route, site, initial-world, frontier, or other hidden-world fact. The contract already defines bounded coordinates, viewport identity/context, stable cell order, typed evidence/source/known-at/freshness data, and a future materialized-cell seam. A future cell requires a closed catalog-owned Jomon glyph reference, an existing `JOMON_PALETTE` semantic token rather than a raw colour, a matching `JOMON_NON_COLOR_STATE_CUES` cue plus text equivalent, authoritative known-source evidence, and structured content-safety provenance. It cannot use this contract to invent a glyph vocabulary; the next item owns actual assignments.

The v1 presentation states are `ready`, `waiting`, `warning`, `risk`, and `neutral`. Each central mapping names one existing semantic palette token and one existing mandatory non-colour cue, so colour is never the sole consequential signal. The present projection contains only three ID-sorted, bounded immediate local status entries—map reservation, courier selection, and current world minute—plus empty message and prompt lists. Status is intentionally not a second management/history ledger. Future messages require a real authoritative source record and typed source kind; future contextual prompts retain only typed option availability/disabled reasons, explicit confirmation and `Escape` cancellation semantics. `cancelTerminalPrompt` produces the explicit zero-time `cancelled-no-mutation` intent and has no reducer authority.

Keyboard input v3 has a canonical conflict-checked table of route-and-focus-specific modes. It records every current canvas key path: worlds/chronicle navigation, `M`/`[`/`]` management controls, `E` chronicle export, `R` advanced-setting reset, `S`/`Enter` profile save, contextual `Enter` selection/settings/creation actions, `Backspace`, and bounded 64-character seed and 32-character profile-name entry. Case-insensitive letter keys and case-preserving bounded text entry are explicit. Binding conflicts are checked only inside the same focused mode, so existing context-specific `Enter` actions do not conflict. Its `worldBindingPreferences` boundary identifies v1 terminal controls as an isolated browser-UI source of effective world bindings; the table retains their validated defaults and keeps `Escape` and `F2` protected. The contract records keyboard-first canvas focus plus pointer-to-focus assistance, text equivalents for map/status/messages/prompts/commands, and a concise accessible summary drawn from the same permitted model. The management sidebar remains a separate household-known strategic surface with no detailed facts duplicated here. The ASCII canvas is the current adapter and a future detailed adapter must consume the same authoritative model, omit no consequential information, invent none, and retain text equivalents.

This is presentation-contract code only: there is no map gameplay, geometry, active prompt, remapping UI, mutable state, persistence, causal command, browser timer, or schema change. `ascii-glyphs.ts` now supplies the next item’s semantic catalogue, but it still creates no map cells. `WorldManifest`, mutable-world, chronicle, settings, palette, BigBlueTerm setup, IndexedDB boundary, and prototype isolation remain unchanged. Focused coverage checks deterministic non-mutating selected-world projection, unmaterialized-map honesty/no hidden-data leakage, canonical bounds, palette/cue/accessibility parity, future cell and evidence rejection, message/prompt source/safety/cancellation, complete real-canvas input-path parity, binding conflict detection, reserved-versus-implemented controls, sidebar separation, and malformed-world/model failure.

Verification on 2026-09-02: `npx vitest run src/medieval/terminal-presentation.test.ts --maxWorkers=1 --no-file-parallelism` passed 6 tests in 1 file (1.46s). Required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 196 tests in 28 files (129.93s). `npm run build` passed TypeScript, Vite (163 modules), and the bundle-size check. `git diff --check` passed after this documentation update. Playwright was not run because no browser-visible production adapter changed. npm emitted the existing unsupported `email` configuration warning; no final required check failed.

- [x] Create an original ASCII glyph vocabulary for terrain, vessel parts, people, goods, work, hazards, weather, and routes; validate unique/legible use in a character cell.

Completed ASCII glyph catalogue decision (2026-09-02): `src/medieval/ascii-glyphs.ts` owns v1 of Jomon’s closed 32-entry semantic catalogue: four each for terrain, vessel parts, adults, goods, work, material hazards, weather, and routes. Every entry has a stable category-prefixed ID, globally unique printable one-cell 7-bit ASCII character, concise original label/text and accessibility equivalent, existing `JOMON_PALETTE` token, required terminal presentation state and matching non-colour cue, future viewport-use allowlist, and a fail-closed content-safety classification/audit. The catalogue contains only semantic references (`semantic-reference-only`); a glyph never reveals a world fact. Future map cells must still use the terminal contract’s source-backed evidence, accessibility text, canonical cell ordering, and palette/cue checks.

The renderer-neutral catalogue converts to the existing `TerminalGlyphCatalog` reference seam, so `validateTerminalMaterializedCells` accepts real glyph IDs without granting the current reserved map any cells. Validation rejects malformed/unknown catalogue versions, malformed IDs/categories, duplicate IDs or characters, noncanonical ordering, absent required category coverage, whitespace/non-ASCII/multicharacter symbols, raw/unknown palette values, mismatched cues, malformed or oversized authored text, invalid future viewport uses, and unclassified or unsafe data. It neither adds a renderer, deck/terrain/person/goods/weather/route data, storage/state schema, causal command, or browser UI nor copies another game’s glyph vocabulary.

Verification for the completed ASCII glyph catalogue: `npx vitest run src/medieval/terminal-presentation.test.ts src/medieval/ascii-glyphs.test.ts --maxWorkers=1 --no-file-parallelism` passed 11 tests in 2 files (1.93s); `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 201 tests in 29 files (98.88s); `npm run build` passed TypeScript, Vite (163 modules), and the bundle-size check; and `git diff --check` passed. The only warning was npm’s existing unsupported `email` configuration warning. Playwright was not run because no browser-visible adapter changed.

- [x] Implement keyboard-first eight-direction movement, compact contextual prompts, remapping, focus handling, command help, and accessible text labels.

Completed terminal-interaction decision (2026-09-02): the renderer-independent `terminal-controls.ts` v1 owns thirteen canonical remappable world bindings: `H`/`J`/`K`/`L` and `Y`/`U`/`B`/`N` for the eight directions, fixed arrow alternatives for cardinal movement, `Enter` contextual prompt, `M` management toggle, `[`/`]` management sections, and `?` help. `F2` opens a compact controls editor; `Enter` captures one bounded, unmodified supported key, `R` resets the selected binding, `A` resets all defaults, and `Escape` is protected and cancels capture/overlays without changing a binding. Case is canonicalized for letters. Unsupported keys, control/alt/meta chords, protected keys, duplicate bindings, malformed preference records, and noncanonical or unknown preference versions reject fail closed. Valid preferences live only in the new `terminal-controls` record/store in medieval IndexedDB database version 3; corrupt records are left untouched and load as defaults, without changing worlds, manifests, chronicles, creation settings, or prototype storage.

World input now resolves through one typed, pure intent resolver while the canvas is focused. Each of the eight directions visibly yields the zero-time `map-reserved.movement-unavailable` result naming its direction; it creates no position, cell, movement, world-time, causal, person, task, social-memory, simulation, era, save, or session change. `Enter` opens the only current contextual prompt, whose sole option is disabled as `no-contextual-action-materialized`; `Enter` can acknowledge that disabled state and `Escape` cancels it first as an explicit zero-time outcome. `?` renders concise effective-binding help, and controls/help overlays also close through `Escape`. Canvas `tabIndex=0`, pointer focus assistance, and a canvas-local key listener ensure keys are handled/prevented only by a focused canvas; pure out-of-focus keys do not hijack page input. Canvas ARIA text and minimal overlay/capture/selected-control/outcome data attributes expose only UI state and the same no-map limitation. The existing 800×630 canvas, semantic palette, BigBlueTerm setup, wrapping, sidebar behavior, and no-colour-only cues remain intact.

This is UI-preference and presentation-contract work only. `FoundationWorld`, immutable generation provenance, mutable-world/chronicle schemas, causal history, settings records, and gameplay state are unchanged; no world migration is performed. No map/deck materialization, turns, travel, prompt action, task/delegation UI, dialogue, trade, combat, or detailed-renderer implementation exists. Focused terminal controls/presentation coverage passed 12 tests in 2 files (1.94s), including bindings, all directions, prompt disable/cancellation, remapping/rejection/reset, and input-contract parity. Required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 207 tests in 30 files (110.14s). `npx playwright test e2e/medieval-foundation.spec.ts --project=chromium` passed 2 tests (4.1s): it covers keyboard Tab and pointer canvas focus, help, disabled prompt, movement result, remap/reload/reset, zero-time IndexedDB world state, existing creation/courier/sidebar behavior, and no external requests. A fresh temporary 1440×900 Chromium screenshot of the controls overlay was inspected: panel borders, wrapped controls, contrast, and explicit text cues were readable, with no artefact retained in the repository. `npm run build` passed TypeScript, Vite (165 modules), and bundle-size checks; `git diff --check` passed. npm emitted the existing unsupported `email` configuration warning, and Playwright emitted its existing `NO_COLOR`/`FORCE_COLOR` warning; no required check failed.

- [x] Specify the future detailed-renderer adapter and its equal-information parity contract, but defer its implementation until Phase 9.

Completed detailed-renderer adapter decision (2026-09-02): `src/medieval/detailed-renderer-adapter.ts` owns v1 of the pure future-renderer boundary. It accepts only a versioned, validated presentation bundle containing `TerminalPresentationModel`, its separate household-known `ManagementSidebarModel`, the existing original ASCII glyph catalogue, and the effective validated v1 terminal-control preferences. It imports no foundation-world, mutable-state, storage, or causal-history authority. The source bundle has a canonical key-order-independent `dr1` fingerprint and identity over exactly those permitted inputs; direct-world-shaped, malformed, unsafe, stale, mixed-world, mismatched, or incompatible sources reject fail closed.

The adapter produces only a renderer-neutral detailed representation and deterministic parity report: each map cell, status/message/prompt item, command/help item, and sidebar fact retains an individually addressable source ID, semantic palette/state and non-colour cue, content classification, source/time/freshness evidence, and same/equivalent accessible text. It detects missing, unexpected, duplicate, stale, source-mismatched, accessibility-altered, semantic/cue-altered, glyph-invalid, hidden-data, unsafe-content, decoration, and input-authority violations with canonical diagnostics. The current map remains exactly the terminal’s zero-cell `reserved-unmaterialized` viewport; the tested materialized-cell fixture is a future source-backed catalogue seam and never materializes a current world. Messages and prompts retain their typed empty/future forms and `Escape` cancellation remains `cancelled-no-mutation`. Sidebar facts stay structurally separate from map/status/messages while remaining parity-covered.

Detailed visual style need not reproduce ASCII pixels or the sixteen-colour appearance, but it must preserve the same consequential information, semantic states, non-colour cues, effective command IDs/availability, cancellation behavior, and accessibility text. Decorations may be only bounded non-authoritative non-textual `frame`/`texture`/`spacing` hints; they cannot carry world meaning. The model has no rendering, click/input execution, timer, world-time, state, persistence, causal-command, or schema authority. `terminal-presentation` remains v3 and `management-sidebar` v1; their new presentation-only structural validators support this adapter without changing their world-facing exact validators. Immutable manifests, mutable-world/chronicle schemas, IndexedDB records, browser UI, font, palette, and prototype isolation remain unchanged. Actual detailed-renderer implementation remains explicitly deferred to Phase 9.

Verification on 2026-09-02: `npx tsc --noEmit && npx vitest run src/medieval/detailed-renderer-adapter.test.ts src/medieval/terminal-presentation.test.ts src/medieval/management-sidebar.test.ts src/medieval/ascii-glyphs.test.ts --maxWorkers=1 --no-file-parallelism` passed 23 tests in 4 files. The required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 215 tests in 31 files (112.59s). `npm run build` passed TypeScript, Vite (165 modules), and the bundle-size check. npm emitted the existing unsupported `email` configuration warning; no required check failed. Playwright was not run because no browser-visible adapter was added.

- [x] Add renderer and browser tests for glyph meaning, prompt cancellation, status/message visibility, keyboard control, and renderer-contract compatibility.

Completed renderer/browser compatibility testing decision (2026-09-02): `renderer-browser-contract.test.ts` supplies the cross-boundary proof over the real, closed 32-entry Jomon ASCII catalogue—not a substitute fixture. It passes every catalogue entry through the future terminal materialized-cell seam and the deferred detailed-adapter bundle, preserving each glyph reference, palette token, semantic state/non-colour cue, accessibility text, and catalogue identity. The fixture is deliberately future-only: selected real worlds remain zero-cell `reserved-unmaterialized` terminal and detailed models, and the joint presentation has no hidden watershed, undisclosed site, person-seed, or ungenerated-frontier-commitment leak. Real-glyph source, cue/state/palette, accessibility, glyph, and duplicate mapping alterations reject through canonical detailed parity diagnostics. The existing adapter remains presentation-bundle-only with no world, persistence, time, or input-execution authority.

The ASCII canvas now consumes the current terminal model for its immediate presentation metadata and explicitly renders `MESSAGES // No current authoritative messages.` when the authoritative message list is empty. It exposes only bounded terminal map/status/message/prompt count/state data attributes and the same immediate map-reservation, active-courier, current-minute, and message-absence facts through its aria label; it does not duplicate household sidebar detail. The browser audit caught a prompt-option row that could paint into the management panel and an omitted accessible capture-rejection result. The canvas now uses the existing bounded wrapping helper for that disabled option and includes current controls/help feedback in its overlay aria summary. No terminal, glyph, detailed-adapter, palette, world, mutable-state, storage, or persistence schema version changed.

Chromium coverage drives the real keyboard canvas through settings/profile editing, generation/courier selection, expanded/collapsed sidebar, every default `Y/K/U/H/L/B/J/N` movement direction plus cardinal arrows, command help, disabled contextual prompt acknowledgement/cancellation, conflict/protected-key capture rejection, remap/reload/reset, blur/refocus, worlds/chronicles navigation and chronicle export, reload, and replacement-page resume. It verifies the empty message/status/prompt accessibility/data state, canvas-local focus handling, no-external-request boundary, and that all UI-only interactions retain the indexed world at minute zero with no additional temporal action, journal, task, autonomy, social-memory, or era state. This completes Phase 1.5 as presentation/testing foundations only: there is still no materialized map/deck cell, detailed renderer, movement/turn, contextual world action, task UI, dialogue, travel, trade, combat, or domain simulation gameplay.

Verification on 2026-09-02: `npx vitest run src/medieval/renderer-browser-contract.test.ts src/medieval/app.test.ts src/medieval/terminal-presentation.test.ts src/medieval/ascii-glyphs.test.ts src/medieval/terminal-controls.test.ts src/medieval/detailed-renderer-adapter.test.ts --maxWorkers=1 --no-file-parallelism` passed 31 tests in 6 files (5.63s). Required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 219 tests in 32 files (95.79s). `npx playwright test e2e/medieval-foundation.spec.ts --project=chromium` passed 2 tests (6.4s). Fresh temporary 1280×720 Chromium screenshots for status/message, disabled prompt, movement feedback, help, and controls were manually inspected: panel boundaries, wrapping, contrast, disabled/action text, and non-colour cues remain clear and within the canvas; no screenshot artifact was added. `npm run build` passed TypeScript, Vite (165 modules), and bundle-size checks. npm emitted the existing unsupported `email` configuration warning; Playwright emitted the existing `NO_COLOR`/`FORCE_COLOR` warning. No verification command failed.

Acceptance: all consequential state can be understood through the ASCII map, status, and message surfaces; the future detailed-view adapter has no authority to hide or invent gameplay information.

#### 1.6 Performance, storage, and diagnostics foundation

- [x] Establish desktop-browser performance and storage budgets for an 8 GB RAM machine. Support current stable desktop Chrome, Edge, Firefox, and Safari through standards-based browser APIs; Chromium is the automated-browser baseline. State the measurement hardware, browser versions, world sizes, population settings, and acceptable interactive responsiveness in the repository.

  Decision (2026-09-02): [`docs/performance-storage-baseline.md`](docs/performance-storage-baseline.md) and the pure `src/medieval/performance-budget.ts` v1 contract define three deterministic current-foundation fixtures: `sheltered-reach-focused` / `performance-sheltered-reach`, `watershed-balanced` / `performance-watershed`, and `far-coast-deep` / `performance-far-coast`. They resolve through the production configuration authority and record the actual initial-world caps (including at most 60 generated person seeds), six instantiated Jomon crew, fidelity budgets, and 1/5/30/120/240-minute scheduler cadences. Person seeds remain distinct from persistent people. The benchmark uses real public APIs only, two warm-ups and nine samples, Node `performance.now()`, linear-interpolated p50/p95, deterministic canonical UTF-8 bytes, no network, IndexedDB, filesystem write, or pre-existing user world. Its `npm run benchmark:medieval-foundation` stdout has both readable lines and `RESULT_JSON`; timing is a review baseline, never a normal-test/CI gate.

  Decision: v1 records conservative future browser review targets (including 2,000 ms cold focused-canvas readiness, 1,500 ms create/save, 250 ms resume and IndexedDB save/load, 500 ms projection/current bounded action, and 50 ms zero-time input) separately from tested deterministic fixture JSON ceilings: active world 512 KiB, terminal controls 4 KiB, creation settings 16 KiB, and a three-world index 64 KiB. The 16/24/32 MiB normal/warning/pressure planning bands are not quota reservations. Valid active worlds and finalized chronicles are never silently deleted; quota/write recovery, snapshots, and backup/export UX remain the next task. `navigator.storage.estimate()` is optional diagnostic-only; no automatic persistence request, quota fill, worker, OPFS, WebGPU, idle-callback, vendor API, browser UA branching, schema change, or save mutation was introduced.

  Decision: Chrome, Edge, Firefox, and Safari stable support is standards/capability based, with Playwright Chromium as the automated baseline and a six-month/change-triggered review. The 2026-09-02 local version inspection found Playwright 1.62.1 Chromium 151.0.7922.34 and Chrome 152.0.7977.66; only the former was launched to identify its version, not performance-tested. Edge, Firefox, and Safari were unavailable on this machine and are correctly marked not-run with their later local-only manual scenario and official release-reference links in the baseline document. This remains an 8 GB planning contract, not evidence of future large-world performance.

  Measurement on 2026-09-02 used a MacBook Pro 15,2, quad-core 2.4 GHz Intel Core i5, 8 GB RAM, macOS 15.7.7 (Darwin 24.6.0), Node v24.19.0, npm 11.17.0, and `tsx` 4.23.1; it contains no unique machine identifier. Across the three fixtures, observed p95 ranges were generation 19.385–35.391 ms, courier selection 77.319–274.350 ms, pure projections 105.715–339.454 ms, and the bounded 240-minute wait 156.241–301.518 ms. Canonical active-world bytes after that wait were 234,703 / 254,485 / 282,385; controls/settings/index were 575 / 620 / 292 bytes. The document contains the full p50/p95/min/max table, resolved settings, and exact method.

  Verification on 2026-09-02: `npx vitest run src/medieval/performance-budget.test.ts --maxWorkers=1 --no-file-parallelism` passed 5 tests in 1 file (1.48s). `npm run benchmark:medieval-foundation` passed and produced the recorded v1 measurements. Required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 224 tests in 33 files (122.70s). `npm run build` passed TypeScript, Vite (165 modules), and bundle-size checks. `git diff --check` passed. Playwright was correctly skipped: this slice has no browser-visible production change. npm emitted the existing unsupported `email` configuration warning; no verification command failed.

- [x] Design compact, indexed local persistence for a multi-world index, active worlds, read-only chronicles, world/person/event/region/task/history records, and exports. Use atomic short-lived writes, quota/error handling, recoverable snapshots, and explicit import/export backups; do not rely on a server, cloud, account, or network connection.

  Decision (2026-09-02): [`docs/persistence-layout.md`](docs/persistence-layout.md) and `src/medieval/persistence-layout.ts` v1 define IndexedDB layout v4 without changing `FoundationWorld`, manifest, mutable-world, chronicle, or terminal-control schemas. The full validated `FoundationWorld` envelope remains the only active mutable-world authority. `world-record-indexes` contains only digest/revision/byte-bound, non-authoritative canonical locators for the envelope, instantiated people, known stored regions, delegation tasks, temporal events, causal tail/checkpoint/segments, and social memory; it never creates independently mutable person/task/history records or a player-knowledge surface.

  Decision: every active-world replacement reads catalog, envelope, derived index, and snapshot ring inside one short-lived transaction, then atomically writes the validated full envelope, catalog, exact index, and any ring update. A different valid previous envelope is copied into a three-entry sequence/slot (`sequence % 3`) full-envelope snapshot ring before replacement. Aborted, quota, request, blocked-upgrade, unavailable-storage, corrupt-envelope/index/ring, and invalid-submission paths have typed failure codes and preserve the old authoritative data. Valid v3 envelopes load unchanged with no metadata rewrite; missing v4 metadata is created only by a later explicit save. Corrupt full records are contained and unavailable, never repaired, migrated, or overwritten by an ordinary save.

  Decision: deterministic ordered-JSON v1 active-world and read-only-chronicle backup bundles contain only validated source data plus non-cryptographic digest/byte identity—no host, browser, machine, wall-clock, or random export data. Imports reject malformed, noncanonical, oversized (over 1 MiB), unsafe, replay-invalid, mixed-ID, digest-mismatched, and collision bundles before mutation. Active-world ID collisions reject by default; explicit replacement snapshots a valid old envelope atomically. Chronicle collisions reject and chronicles remain read-only. Snapshot inspection is pure and restore is explicit. Preflight byte results use the Phase 1.6 planning bands as advisory only; no quota fill, automatic persistence request, cleanup, unload write, cloud, account, server, or network path exists. UI recovery/backup affordances and split-record optimization remain deferred.

  Verification on 2026-09-02: focused `persistence-layout.test.ts` passed 4 tests in 1 file (7.39s); targeted storage layout regressions passed 6 tests with 31 unrelated tests skipped (17.09s). Required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 234 tests in 34 files (121.14s). `npm run benchmark:medieval-foundation` completed: active-world bytes remained 234,703 / 254,485 / 282,385 for sheltered/watershed/far-coast fixtures; this run's bounded 240-minute wait p95 values were 244.346 / 565.046 / 154.655 ms and remain review observations, not CI gates. `npm run build` passed TypeScript, Vite (167 modules), and bundle-size checks. `git diff --check` passed before this documented completion and is rerun below. Playwright was correctly skipped because no browser-visible behavior changed. npm emitted the existing unsupported `email` configuration warning; no required verification command failed.

- [x] Define profile-guided optimization boundaries: spatial indexing, incremental generation, deterministic scheduled summaries, packed/compact data where measured, memoization, and worker-based generation/simulation only when they preserve reproducibility and readable ownership.

  Decision (2026-09-03): [`docs/optimization-boundaries.md`](docs/optimization-boundaries.md) and the pure `src/medieval/optimization-boundaries.ts` v1 contract make the present foundation fail closed and unable to authorize an optimization. It binds reproducible current fixtures to the validated full-envelope identity (world ID, causal revision, digest, canonical bytes, and relevant contract versions), rejects malformed/incomplete/noncanonical/stale/mismatched evidence, and records all six strategies as not yet eligible with exact future owners, measured-pressure/equivalence prerequisites, and current blockers. The current basis is intentionally narrow: terminal presentation has zero materialized map cells; initial/frontier commitments are bounded; catch-up remains canonical scheduling/provenance rather than domain simulation; and `FoundationWorld` remains sole mutable authority.

  Decision: any future derived index, compact/packed projection, memoization entry, or worker result is read-only and known-facts-only, carries a precise full-envelope binding and positive derived contract version, and invalidates/rebuilds on any identity/version mismatch. Memoization is limited to caller-owned, bounded-lifetime pure projections with complete keys and no retained player knowledge/world facts. The optional-worker request/result types are only a future adapter boundary: versioned authority-provided canonical input, named deterministic RNG streams where required, no IndexedDB/mutable-world/simulation/persistence authority, stale-result rejection, and exact validated main-thread equivalence. Cancellation, unsupported capability, timeout, or failure cannot mutate or advance a world. Scheduled-summary batching remains future-only and must preserve existing canonical cursor/window ordering, cadence, outcome evidence, bounded records, and partition invariance; it does not add economy, institution, people, travel, or combat simulation.

  Compatibility: no `FoundationWorld`/manifest/world schema change, save migration, IndexedDB layout change (still v4), persistent cache store, worker/build configuration, binary serialization, grid, packed live state, incremental generation, browser behavior, or full domain simulation. Valid v3 envelopes still load unchanged and layout metadata remains explicit-save-only.

  Verification (2026-09-03): final focused `npx vitest run src/medieval/optimization-boundaries.test.ts src/medieval/performance-budget.test.ts src/medieval/persistence-layout.test.ts src/medieval/storage.test.ts src/medieval/simulation-catchup.test.ts --maxWorkers=1 --no-file-parallelism` passed 59 tests in 5 files (64.94s). Final required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 240 tests in 35 files (136.17s). `npm run benchmark:medieval-foundation` passed; active-world deterministic bytes remained 234,703 / 254,485 / 282,385 (all within 512 KiB), while this review-only run's p95 ranges were generation 20.741–58.104 ms, courier selection 86.634–120.062 ms, pure projections 108.268–337.112 ms, and bounded 240-minute waits 284.145–410.628 ms. `npm run build` passed TypeScript, Vite (167 modules), and bundle-size checks; `git diff --check` passed. Playwright was correctly skipped because this slice has no browser-visible production change; no browser/quota measurement was invented. npm emitted the existing unsupported `email` configuration warning. One interim full-suite retry hit the existing `storage.test.ts` recurring-social-refusal test's declared 20-second timeout (23.9s); its isolated diagnostic rerun passed in 15.25s and the final exact full suite above passed.

- [x] Add performance fixtures across generation presets and simulation-fidelity settings, with regression checks for memory growth, persistence size, generation time, due-event simulation time, UI responsiveness, and multi-world/chronicle index operations.

  Decision (2026-09-03): `src/medieval/performance-fixtures.ts` v1 adds the deterministic, in-memory fixture layer without changing any authority or storage contract. It crosses sheltered-reach, watershed, and far-coast with focused, balanced, and deep fidelity through the production resolver. The established diagonal fixture identities/seeds remain exactly `sheltered-reach-focused` / `performance-sheltered-reach`, `watershed-balanced` / `performance-watershed`, and `far-coast-deep` / `performance-far-coast`; the six new stable rows are `sheltered-reach-balanced`, `sheltered-reach-deep`, `watershed-focused`, `watershed-deep`, `far-coast-focused`, and `far-coast-balanced` with named preset/fidelity seeds. Each scenario builds a validated initial world, chooses `crew:0`, projects terminal/sidebar/detailed-adapter data without changing world time or materializing map cells, and compares a valid `60 + 180` minute due-event sequence to one 240-minute action across the existing 1/5/30/120/240 cadence families.

  Decision: deterministic regression assertions cover fixture identity/order, resolver output, full-envelope canonical bytes, retained-state deltas, scheduled-summary projection partition equivalence, zero-time pure projection equivalence, and real pure catalogue operations over three active full envelopes plus four read-only chronicles. The after-sequence full envelopes are 255,631–313,434 canonical bytes and all remain below the existing 512 KiB enforced ceiling; terminal controls, the explicit-save-only three-profile settings record, and the bounded catalogue index are respectively 575/4 KiB, 620/16 KiB, and 635/64 KiB. These bytes and retained-state counts are deterministic growth proxies only—not browser RAM, browser quota, IndexedDB capacity, or process memory. Node heap/RSS is intentionally not collected. Node `performance.now()` generation, courier, projection, one-action, and due-event-sequence observations remain review-only; the benchmark's 2026-09-03 p95 ranges are documented in [`docs/performance-storage-baseline.md`](docs/performance-storage-baseline.md).

  Compatibility: no `FoundationWorld`, manifest, world, chronicle, or persistence schema changed; IndexedDB remains layout v4, valid v3 envelopes still load unchanged, and layout metadata remains explicit-save-only. No persistent cache/store, worker/build configuration, spatial index, binary/packed live state, incremental generation, migration, browser feature, user save dependency, or domain simulation was added. The optimization-boundaries worker request now uses locale-independent ordinal ordering for canonical named RNG streams; it remains a future adapter contract only and adds no Worker code. The two specifically identified expensive tests—the delegation canonical-due-minute case and storage quota/abort containment case—now each have an explicit 20-second local timeout. This is test-runtime stabilization, not a performance claim.

  Verification (2026-09-03): `npx vitest run src/medieval/performance-fixtures.test.ts src/medieval/performance-budget.test.ts src/medieval/optimization-boundaries.test.ts src/medieval/persistence-layout.test.ts src/medieval/storage.test.ts src/medieval/simulation-catchup.test.ts src/medieval/delegation.test.ts --maxWorkers=1 --no-file-parallelism` passed 74 tests in 7 files (93.26s). Final `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 245 tests in 36 files (152.15s). `npm run benchmark:medieval-foundation` passed with all nine fixtures, seven operations, two warm-ups, nine samples, in-memory-only persistence, and structured deterministic/review-only labels. `npm run build` passed TypeScript, Vite (167 modules), and bundle-size checks; `git diff --check` passed. Playwright was correctly skipped because no browser-visible production behavior changed.

  Warnings and limitations: npm emitted the existing unsupported `email` configuration warning. One interim full-suite attempt had two unrelated existing `storage.test.ts` timeout failures (default 5 seconds and an existing 20 seconds); each passed isolated (2.23s and 11.58s) and the unchanged final required full suite passed. Browser responsiveness, browser memory/quota, and real IndexedDB timing remain deliberately unmeasured; no result claims them.

- [x] Do not add user mod/content-pack compatibility. Keep internal content data-driven, validated, and documented without committing to a public extension surface.

  Decision (2026-09-03): [`docs/internal-content-boundary.md`](docs/internal-content-boundary.md) and the pure `src/medieval/internal-content.ts` v1 contract define one closed, application-owned eight-family catalogue. It references—not duplicates—the authoritative content-safety, generation-config, initial-world, frontier, ASCII-glyph, delegation, and world owners. Descriptors distinguish developer-authored compiled data, deterministic generator input, renderer semantics, runtime task definitions, generated world records, and explicitly forbidden user/public extension input. Each carries current owner-version reference, fail-closed safety requirement, source-impact category, visibility rule, and required owner compatibility/replay decision.

  Decision: the catalogue has no mutable registry and returns only fresh copies of its fixed descriptor set. Its validator rejects malformed, duplicate/noncanonical, unknown-owner/category, missing/unsafe-safety, invalid-impact/change-rule, and extension-shaped inputs. The extension policy explicitly forbids user content, runtime loaders, dynamic modules, executable hooks, browser/network fetch, persistence registries, and any public compatibility promise. Existing owner validators/resolvers remain authoritative; the new owner-reference check merely confirms their live static data is recognized. Generated data stays content-safety classified, deterministic, bounded, known-facts-only where applicable, and free of sexual violence, slavery, torture, and harm/endangerment of children.

  Compatibility: no authored owner value, array order, seed/RNG stream, generation result, glyph assignment, task behavior, player-facing text, `FoundationWorld`, manifest, replay contract, save schema, IndexedDB layout v4, or valid v3-envelope loading behavior changed. A future generator-consumed content change requires an explicit generator/provenance recreation decision; a future task/presentation contract change requires its owning contract-version/replay decision. This slice adds neither a migration nor a compatibility shim.

  Verification (2026-09-03): `npx vitest run src/medieval/internal-content.test.ts src/medieval/content-safety.test.ts src/medieval/initial-world.test.ts src/medieval/frontier.test.ts src/medieval/ascii-glyphs.test.ts src/medieval/delegation.test.ts src/medieval/world-manifest.test.ts --maxWorkers=1 --no-file-parallelism` passed 44 tests in 7 files (11.68s). Required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 249 tests in 37 files (147.08s). `npm run build` passed TypeScript, Vite (167 modules), and bundle-size checks. `git diff --check` passed. `npm run benchmark:medieval-foundation` was correctly skipped because performance/persistence behavior did not materially change; Playwright was correctly skipped because no browser-visible production behavior changed.

  Warnings and unresolved limitations: npm emitted the existing unsupported `email` configuration warning. There remains intentionally no user mod/content-pack API, loader, or compatibility promise; future owner-content changes still require the explicit versioning/provenance/replay decisions above.

Acceptance: declared 8 GB baseline fixtures generate, simulate, save, reload, and remain responsive within recorded budgets; quota or write failure preserves the last good state and offers recovery/export.

#### 1.7 Effects, recovery, and irreversible-loss foundation

- [x] Define a renderer-independent effect model for health, injury, exhaustion, preparation, equipment, relics, totems, boons, curses, crew support, enemy weaknesses, environmental interactions, duration, stacking, chaining, and counterplay.

  Decision (2026-09-03): `src/medieval/effects.ts` v1 is a bounded, pure, renderer-independent semantic resolver. Its closed source vocabulary covers health, injury, exhaustion, preparation, equipment, grounded relics/totems, boons/curses, crew support, enemy weaknesses, and environmental interactions without introducing actual combat, health recovery, social control, spellcasting, or mutable gameplay. A caller supplies only revision-bound facts already known to it, canonical action-world minutes, safety-classified candidates, and explicit known counterplay selections. Resolution returns canonical semantic sources, targets, evidence IDs, group totals, suppression reasons, and explanations; it cannot invent a hidden fact or grant universal persuasion/control.

  Decision: durations are inclusive at their canonical action-world start minute and exclusive at expiry; pure calls never advance time. Canonical order is chain depth, descending priority, then ordinal ID; explicit reciprocal conflicts retain the higher-priority/ordinal winner, stack groups share target/polarity/mode/cap, and a suppressed prerequisite suppresses its dependent candidate. The v1 limits are 32 known facts, 24 effects/selections, four evidence/stack contributors, eight conflicts, four prerequisites/depth, magnitude three, priority nine, and 1,440 minutes. Malformed, unsafe, stale, ineligible, duplicate, unknown, noncanonical, conflicting, and cyclic inputs reject fail-closed. The new document [`docs/effects-model.md`](docs/effects-model.md) records ownership, grounding, safety, no-hidden-knowledge rules, and all non-goals.

  Compatibility: this changes no authored content, `FoundationWorld`, world/manifest/replay schema, mutable-world owner, IndexedDB layout v4, derived index, save envelope, valid v3-envelope loading, or explicit-save-only metadata. It adds no migration, shim, persistence/cache/index, worker, random stream, packed data, spatial system, UI/renderer behavior, gameplay action, actual simulation, or public extension surface. `temporal.ts`, `world.ts`/`world-state.ts`, `causal-history.ts`, content safety, and presentation owners remain authoritative. The companion mystical-policy contract now constrains future relic/totem/boon/curse rarity, source, cost, condition, and audit evidence without adding content or gameplay.

  Verification (2026-09-03): `npx vitest run src/medieval/effects.test.ts src/medieval/content-safety.test.ts src/medieval/temporal.test.ts src/medieval/world-state.test.ts src/medieval/causal-history.test.ts src/medieval/persistent-person.test.ts src/medieval/internal-content.test.ts --maxWorkers=1 --no-file-parallelism` passed 47 tests in 7 files (12.88s). Final `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 254 tests in 38 files (158.65s). `npm run build` passed TypeScript, Vite (167 modules), and bundle-size checks; `git diff --check` passed. `npm run benchmark:medieval-foundation` was skipped because performance/persistence behavior did not materially change; Playwright was skipped because no browser-visible production behavior changed.

  Warnings and limitations: npm emitted the existing unsupported `email` configuration warning. One interim serial full-suite run timed out in four pre-existing expensive fixture/replay tests; isolated reruns passed (25 tests), and the final two required full-suite runs passed. The model intentionally has no persistence or gameplay integration and is not authorization for combat, recovery, death/revival, Jomon loss, mystical rarity/audit rules, or public mods/content packs.
- [x] Replace the fixed sixteen-colour terminal palette with an original full-colour dark autumnal semantic palette: warm low-glare surfaces, parchment text, moss and river accents, and restrained amber/rust risk cues. Apply it across every current medieval browser surface while preserving semantic roles, non-colour cues, ASCII/keyboard readability, renderer parity, accessibility contrast, and all domain/persistence boundaries.

  Decision (2026-09-03): `src/medieval/palette.ts` v2 replaces the fixed historical sixteen-colour table and its types/tests with one original 24-bit semantic palette. It owns the raw soot/olive ground and surface values, parchment text, moss readiness/action values, river navigation values, amber warning, and restrained rust risk/error values. Existing semantic token names remain the renderer-neutral boundary. Browser bootstrap derives every CSS custom property and the `theme-color` metadata from that sole palette source; the canvas uses the same semantic surface, border, text, status, warning, and error roles. [`docs/semantic-palette.md`](docs/semantic-palette.md) records role ownership, original-inspiration boundary, deterministic contrast requirements, and non-goals.

  Accessibility and parity: the versioned deterministic WCAG contrast matrix requires all eleven current small-text roles to meet 4.5:1 against the canvas panel surface. It requires the panel border against both adjacent dark surfaces and selection/water/route/ready/waiting/risk graphical cues against both dark surfaces to meet 3:1. Existing ASCII glyphs, text equivalents, ARIA labels, keyboard behavior, selection marker, state prefixes, and renderer-parity semantic-token rules are unchanged, so colour is never the only consequential signal. Fresh temporary Chromium screenshots of the controls and chronicle states were manually inspected for text legibility, panel separation, teal selection, amber warning, and rust risk visibility; no screenshots were retained in the repository.

  Compatibility: this is browser presentation and metadata only. It changes no authored world value, seed/RNG stream, generation output, glyph assignment, terminal/detailed projection semantics, gameplay, `FoundationWorld`, manifest, replay, save envelope, valid v3 loading, IndexedDB layout v4, or persistence schema. No migration, compatibility shim, palette preference, theme selector, user input, public extension surface, worker, cache, or performance/persistence change was added.

  Verification (2026-09-03): `npx vitest run src/medieval/palette.test.ts src/medieval/app.test.ts src/medieval/terminal-presentation.test.ts src/medieval/ascii-glyphs.test.ts src/medieval/detailed-renderer-adapter.test.ts src/medieval/renderer-browser-contract.test.ts --maxWorkers=1 --no-file-parallelism` passed 30 tests in 6 files (5.74s). `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 254 tests in 38 files (141.38s). `npx playwright test e2e/medieval-foundation.spec.ts --project=chromium` passed 2 tests (19.7s), including palette CSS-variable, browser metadata, and canvas ground/panel-pixel assertions. `npm run build` passed TypeScript, Vite (167 modules), and bundle-size checks. `git diff --check` passed. `npm run benchmark:medieval-foundation` was correctly skipped because the presentation-only palette change did not materially alter performance or persistence behavior.

  Warnings and limitations: npm emitted the existing unsupported `email` configuration warning; Playwright also reported the existing `NO_COLOR`/`FORCE_COLOR` environment warning. Contrast is deterministically checked for the declared current surfaces, and the inspection was a Chromium screenshot review; this slice does not claim broader browser, display, colour-vision, or user-preference testing. There remains intentionally no palette chooser or public theme/content extension surface.
- [x] Define rarity, source, cost, condition, and audit rules for mystical effects. They must remain finite, legible, in-world, and compatible with low-mysticism tone; no generic mage class is introduced.

  Decision (2026-09-03): `src/medieval/mystical-effect-policy.ts` v1 is a pure, bounded, compiled-TypeScript policy/audit contract. It adds no authored mystical definition, generator, registry, resolver, or mutable state. Its closed low-mysticism ladder is scarce, rare, exceptional, then ultra-rare; it has no common tier. Definitions declare finite per-world discovery/concurrent-eligibility ceilings (scarce 6/2, rare 3/1, exceptional 1/1, ultra-rare 1/1) as inspectable policy ceilings only, not spawning or discovery behavior.

  Decision: every future relic, totem, boon, or curse policy definition must bind an existing `effects.ts` semantic candidate and revisioned known facts. The audit delegates candidate-category, duration, grounding, stack, chain, counterplay, and content-safety validation to that owner; it never resolves or applies the candidate. Material objects, specific places, bounded practices, and established crew relationships are the only grounded source classes. They require matching known-fact source provenance, source-class conditions, finite duration or limited-use boundary, one to three cost/trade-off references, and bounded audit evidence for source, conditions, availability, costs, and adverse counterplay. Relationship-bound boons use the existing crew-support category and cannot imply universal persuasion or control. Malformed, common, free, unlimited, ungrounded, stale, duplicate, noncanonical, contradictory, unsafe, and unauditable definitions fail closed with typed stable diagnostics. [`docs/mystical-effect-policy.md`](docs/mystical-effect-policy.md) records the full matrix and deferred ownership.

  Safeguard boundary: ultra-rare definitions may carry only a `deferred-no-authority` reservation for a future courier-loss or Jomon-loss safeguard. It grants no gameplay behavior and cannot authorize generic mages, spell lists, arbitrary casting, unlimited power, coercive social control, death prevention, revival, Jomon rescue/collapse prevention, or terminal-loss override. The contract creates no child participant or player-facing content and retains affirmative exclusion of sexual violence, slavery, torture, and harm/endangerment of children.

  Compatibility: no existing authored data, seed/RNG stream, generation/frontier result, `FoundationWorld`, mutable-world owner, manifest, replay, causal history, save envelope, IndexedDB layout v4, valid v3 loading, schema, or migration changed. The fixed internal-content catalogue was intentionally not expanded. This adds no worker, cache, packed data, UI, renderer, browser path, combat, recovery, death, Jomon integrity, inventory, cost execution, world integration, or public mod/content-pack surface. Future generation/provenance and any durable implementation remain deferred to their owning systems.

  Verification (2026-09-03): `npx vitest run src/medieval/mystical-effect-policy.test.ts src/medieval/effects.test.ts src/medieval/content-safety.test.ts src/medieval/temporal.test.ts src/medieval/world-state.test.ts src/medieval/causal-history.test.ts src/medieval/internal-content.test.ts --maxWorkers=1 --no-file-parallelism` passed 43 tests in 7 files (9.91s). `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 259 tests in 39 files (174.11s). `npm run build` passed TypeScript, Vite (167 modules), and bundle-size checks. `git diff --check` passed. Playwright was correctly skipped because no browser-visible production behavior changed; `npm run benchmark:medieval-foundation` was correctly skipped because performance and persistence behavior did not materially change.

  Warnings and limitations: npm emitted the existing unsupported `email` configuration warning. This is an audit-only policy boundary: it does not prove a future generated, discovered, inventory-held, spent, or applied mystical effect, nor a future loss safeguard, is implemented. Broader gameplay, browser, and performance measurements remain intentionally out of scope.
- [x] Define courier death, prevention, revival, household-extinction, and lasting-consequence rules. Revival safeguards are exceptional and explicit; the default outcome remains permanent loss. When no eligible living crew member remains, finalise the world as a read-only, exportable chronicle.

  Decision (2026-09-03): `src/medieval/courier-loss-policy.ts` v1 is a pure, renderer-independent assessment only. It consumes a read-only persistent-person validation context/snapshot, canonical action-world minute, static crew eligibility, content-safety classification, effects known-fact vocabulary, and the existing `crew-extinction` chronicle reason. A valid confirmed loss requires the matching eligible courier's validated persistent person to be dead at that exact canonical minute. The result is deterministic, bounded, ordinal-ID ordered, and contains only loss/evidence IDs, permanent-loss reason codes, successor eligibility facts, safeguard status, consequences, and a possible finalization intent.

  Decision: confirmed courier loss is always `permanent-loss-required`. Future owners must retain death, and must not silently create a replacement, resurrect someone, transfer possessions, reassign tasks, erase relationships, or rewrite history. Immediate successors are eligible living crew currently available; the broader eligible-living set additionally retains committed/unavailable survivors as temporarily unavailable. Only the empty broader set emits the existing `crew-extinction` read-only-chronicle finalization intent. It does not select a successor, mutate a person/world, append history, call `finalizeWorldAsChronicle()`, or finalize storage.

  Safeguard boundary: prevention and revival are both `unavailable` by default and remain unavailable even when the input cites a revision-matched, existing mystical-policy v1 ultra-rare `future-courier-loss-safeguard` audit record. That record supplies future provenance only, never an outcome. Malformed, unsafe, stale, duplicate, noncanonical, child-related, ungrounded, unauthorized, impossible, or contradictory input rejects fail-closed with typed diagnostics. There is no generic mage, spellcasting, automatic safety net, hidden-person creation, or hidden-world-knowledge output. [`docs/courier-loss-policy.md`](docs/courier-loss-policy.md) records ownership and deferred integration.

  Compatibility: no `FoundationWorld`, `WorldChronicle`, person/world/manifest/replay/causal-history contract, save envelope, IndexedDB v4 layout, valid-v3 loading behavior, schema, migration, storage transaction, generated content, RNG stream, UI, renderer, combat, recovery, death reducer, revival action, courier switch, cache, worker, or prototype behavior changed. The existing persistent-person, effects, mystical-policy, chronicle, and content-safety owners remain authoritative. This adds no child participant and retains affirmative exclusion of sexual violence, slavery, torture, and harm/endangerment of children.

  Verification (2026-09-03): `npx vitest run src/medieval/courier-loss-policy.test.ts src/medieval/persistent-person.test.ts src/medieval/world-state.test.ts src/medieval/world.test.ts src/medieval/storage.test.ts src/medieval/causal-history.test.ts src/medieval/effects.test.ts src/medieval/mystical-effect-policy.test.ts --maxWorkers=1 --no-file-parallelism` passed 90 tests in 8 files (83.32s). `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 267 tests in 40 files (156.32s). `npm run build` passed TypeScript, Vite (167 modules), and bundle-size checks; `git diff --check` passed. Playwright was correctly skipped because no browser-visible production behavior changed; `npm run benchmark:medieval-foundation` was correctly skipped because performance and persistence behavior did not materially change.

  Warnings and limitations: npm emitted the existing unsupported `email` configuration warning. This is policy assessment only: it does not make or persist a death, perform a prevention/revival, select a successor, apply household consequences, or create a chronicle. Those actions require future owner-specific causal, replay, safety, persistence, and schema decisions.
- [x] Define Jomon integrity, partial disaster, repair, rescue, collapse, and loss rules. A relic or equivalent able to save Jomon from terminal loss is ultra-rare and must have a visible causal chain, cost, and recovery trade-off. Terminal loss ends active play and finalizes a read-only, exportable chronicle.

  Decision (2026-09-03): `src/medieval/jomon-integrity-policy.ts` v1 is a pure, renderer-independent assessment boundary over a read-only, validated `WorldJomonState`, canonical action-world minute, existing effects known facts, content-safety metadata, and an optional existing mystical-policy audit record. It uses integer-only bands (`sound`, `weathered`, `damaged`, `critical`, `collapsed`) and ordinal-ID ordering. Ordinary condition has no incident evidence; partial disaster requires bounded grounded causal, cost, and recovery-trade-off evidence; only a collapsed terminal incident with explicit collapse evidence emits the existing `jomon-loss` `read-only-chronicle-finalization-intent`. The assessment neither calls finalization nor changes a world, history, save, or person.

  Deferred ownership: future repair and non-mystical rescue owners must supply grounded cause, material/labour/time/risk cost, recovery trade-off, and an inspectable causal chain. A revision-matched existing mystical-policy v1 ultra-rare `future-jomon-loss-safeguard` reservation can be inspected only as future provenance and remains unavailable/deferred; it cannot prevent collapse, repair or revive Jomon, or override terminal loss. Malformed, stale, duplicate, noncanonical, unsafe, ungrounded, contradictory, impossible, or unauthorized inputs reject fail-closed with typed diagnostics. Jomon loss stays separate from courier loss; no courier, possession, task, or hidden-world fact is selected, transferred, or fabricated. [`docs/jomon-integrity-policy.md`](docs/jomon-integrity-policy.md) defines ownership, evidence, compatibility, and finalization-intent-only semantics.

  Compatibility: no `FoundationWorld`, `WorldChronicle`, manifest, replay, causal-history, persistent-person, effects/mystical/courier policy owner, world schema, save envelope, IndexedDB v4 layout, valid-v3 loading behavior, migration, transaction, RNG stream, generated content, UI, renderer, worker, cache, packed data, browser behavior, or prototype code changed. This creates no disaster/repair/rescue/death gameplay, integrity mutation, storage finalization, or public extension surface, and retains affirmative exclusion of sexual violence, slavery, torture, and harm/endangerment of children.

  Verification (2026-09-03): `npx vitest run src/medieval/jomon-integrity-policy.test.ts src/medieval/mystical-effect-policy.test.ts src/medieval/courier-loss-policy.test.ts src/medieval/effects.test.ts src/medieval/world-state.test.ts src/medieval/world.test.ts src/medieval/storage.test.ts src/medieval/causal-history.test.ts src/medieval/temporal.test.ts src/medieval/content-safety.test.ts --maxWorkers=1 --no-file-parallelism` passed 105 tests in 10 files (73.83s). The final required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 274 tests in 41 files (151.86s). `npm run build` passed TypeScript, Vite (167 modules), and bundle-size checks; `git diff --check` passed. Playwright was correctly skipped because no browser-visible production behavior changed; `npm run benchmark:medieval-foundation` was correctly skipped because performance and persistence behavior did not materially change.

  Warnings and limitations: npm emitted the existing unsupported `email` configuration warning. One interim serial full-suite attempt hit unchanged default five-second timeouts in causal-history compaction and delegation/social-hardening tests; both passed unchanged in isolation, and the final required serial wildcard run passed. This boundary does not create disasters, apply repairs/rescues, advance time, change integrity, end active play, or export/finalize a chronicle; those actions remain with their future causal, world, replay, and storage owners.
- [x] Add deterministic tests for effect combinations, cap/chain behavior, death prevention/revival, Jomon collapse, terminal-world finalization/chronicle export, and state recovery.

  Decision (2026-09-03): `src/medieval/effects-loss-recovery.test.ts` adds four deterministic cross-contract cases over only existing owners. It proves representative effect combinations retain canonical caps, reciprocal conflicts, chain ordering/cycle rejection, counterplay, action-world expiry, input immutability, and no wall-clock, locale, or unseeded-randomness dependency. It composes valid ultra-rare future reservations with the courier-loss and Jomon-integrity assessments to prove prevention, revival, rescue, and terminal-loss override remain unavailable; policy outputs remain permanent-loss/future-only intent and do not select successors, restore people, transfer work/possessions, rewrite history, mutate Jomon, or create a chronicle.

  Explicit ownership: after a valid `jomon-loss` finalization intent is assessed, the test explicitly invokes the existing `finalizeWorldAsChronicle(world, 'jomon-loss')` owner and checks its clone-preserving, deterministic, parseable export. It does not imply a production policy-to-world or policy-to-storage handoff. A focused `storage.test.ts` addition uses a real replacement/snapshot fixture to prove stale finalization and corrupt snapshot sources fail closed without overwriting the valid active envelope, catalog, or corrupt stored record. [`docs/jomon-integrity-policy.md`](docs/jomon-integrity-policy.md) and [`docs/persistence-layout.md`](docs/persistence-layout.md) document that distinction.

  Compatibility: test and documentation coverage only; no production resolver, policy, `FoundationWorld`, `WorldJomonState`, chronicle, manifest, causal history, save envelope, IndexedDB v4 layout, valid-v3 loading, schema, migration, storage behavior, UI, renderer, RNG stream, worker, cache, or gameplay behavior changed. There is still no automated loss-policy orchestration, death/revival/recovery reducer, Jomon repair/rescue/damage action, or browser recovery UI.

  Verification (2026-09-03): `npx vitest run src/medieval/effects-loss-recovery.test.ts src/medieval/effects.test.ts src/medieval/mystical-effect-policy.test.ts src/medieval/courier-loss-policy.test.ts src/medieval/jomon-integrity-policy.test.ts src/medieval/world.test.ts src/medieval/world-state.test.ts src/medieval/storage.test.ts src/medieval/persistence-layout.test.ts src/medieval/causal-history.test.ts src/medieval/temporal.test.ts src/medieval/content-safety.test.ts --maxWorkers=1 --no-file-parallelism` passed 114 tests in 12 files (80.67s). `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 279 tests in 42 files (158.93s). `npm run build` passed TypeScript, Vite (167 modules), and bundle-size checks; `git diff --check` passed. Playwright was correctly skipped because no browser-visible production behavior changed; `npm run benchmark:medieval-foundation` was correctly skipped because no material performance or persistence behavior changed.

  Warnings and limitations: npm emitted the existing unsupported `email` configuration warning. An interim ad-hoc storage run hit the pre-existing 20-second timeout in the recurring social-refusal compaction test; the required focused command and final full serial suite passed unchanged. These tests do not implement the deferred actions or claim browser/IndexedDB timing coverage.

Acceptance: a player can inspect why an effect or recovery occurred; every powerful safeguard has an explicit rarity/cost/counterplay contract; terminal Jomon loss ends active play cleanly, preserves its history as a read-only chronicle, and offers export without corrupting the record.

### 2. Physical Jomon foundation

#### 2.1 Walkable Jomon and quay

- [x] Create a compact, original ASCII deck plan with a connected quay approach, gangplank, tavern, chart table, cargo hold, repair space, stores, berths, and galley.

  Decision (2026-09-03): `src/medieval/jomon-deck-plan.ts` v1 derives a pure, bounded 18 by 8 static future-map input only from a validated `FoundationWorld`/existing `FoundationJomon`. It has exactly nine canonical ordinal-ID areas (`quay-approach`, gangplank, tavern, chart table, cargo hold, repair space, stores, berths, and galley), a non-overlapping hull boundary, and verified orthogonal quay-to-gangplank-to-vessel access. It binds only the existing `prop:gangplank`, `prop:task-ledger`, and `prop:chart-table` to their existing partition/kind and static anchor; it creates no other prop or operation. The local quay approach is deliberately neither a site, route, frontier fact, travel destination, nor persistent partition. [`docs/jomon-deck-plan.md`](docs/jomon-deck-plan.md) records its ownership, semantic references, validation, and deferred work.

  Boundaries and compatibility: the plan reuses closed `future-jomon-deck` glyph references, semantic palette tokens, paired non-colour cues, existing accessible/text equivalents, and affirmative content-safety classifications. It remains a discardable derived contract, not a terminal-cell map: `createTerminalPresentationModel()` is unchanged and still returns the zero-cell `reserved-unmaterialized` map; detailed-renderer parity remains deferred. No `FoundationWorld`, `WorldJomonState`, manifest, replay, causal history, save envelope, IndexedDB v4 layout, valid-v3 load, migration, browser UI, renderer, world time, RNG, worker, cache, persistence, gameplay, content catalogue, or public extension/mod surface changed.

  Verification (2026-09-03): `npx vitest run src/medieval/jomon-deck-plan.test.ts src/medieval/world.test.ts src/medieval/world-state.test.ts src/medieval/ascii-glyphs.test.ts src/medieval/terminal-presentation.test.ts src/medieval/detailed-renderer-adapter.test.ts src/medieval/content-safety.test.ts --maxWorkers=1 --no-file-parallelism` passed 51 tests in 7 files (10.68s). `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 284 tests in 43 files (156.52s). `npm run build` passed TypeScript, Vite (167 modules), and bundle-size checks; `git diff --check` passed. Playwright was skipped because this layout-only contract makes no browser-visible production change. `npm run benchmark:medieval-foundation` was skipped because neither performance nor persistence behavior changed.

  Warnings and limitations: npm emitted the existing unsupported `email` configuration warning. This task does not materialize/render map cells, implement movement/collision/camera/visibility, operate props, prompt, travel, crew switching, cargo/repair actions, or create any quay/world record; those remain future owners.
- [x] Render the plan in the primary ASCII mode from the common map state; document its original glyph vocabulary and preserve the Phase 1 detailed-renderer adapter contract.

  Decision (2026-09-03): terminal-presentation v4 materializes exactly the validated 18 by 8 Jomon deck-plan cells as the one renderer-independent common terminal map: 113 source-backed area and hull cells in canonical row/column order. The primary canvas reads that terminal map and the closed glyph catalogue rather than owning deck geometry; it visibly draws only `)` quay approach, `/` gangplank, `=` open deck, and `#` hull planking. Each cell retains its catalogue-owned semantic palette token, paired non-colour cue, text/accessibility equivalent, affirmative safety classification, and source evidence. The detailed-renderer adapter receives the same terminal projection and remains a Phase 1 parity-only adapter.

  Boundaries and compatibility: this is a derived terminal-contract/browser rendering change only, with no `FoundationWorld`, `WorldJomonState`, manifest, replay, causal history, save envelope, IndexedDB v4 layout, valid-v3 loading behavior, schema, migration, persistence, RNG, worker, cache, glyph catalogue, palette contract, or public extension surface change. The local quay remains non-persistent. Movement stays zero-time `jomon-deck.movement-unavailable`; the disabled contextual prompt has no action. No courier position, cargo, person/NPC, terrain, route-travel, interaction, collision, camera, visibility, or prop operation state is materialized. [`docs/jomon-deck-plan.md`](docs/jomon-deck-plan.md) now records the original glyph vocabulary, semantic/accessibility ownership, and remaining deferrals.

  Verification (2026-09-03): `npx vitest run src/medieval/jomon-deck-plan.test.ts src/medieval/terminal-presentation.test.ts src/medieval/ascii-glyphs.test.ts src/medieval/palette.test.ts src/medieval/detailed-renderer-adapter.test.ts src/medieval/renderer-browser-contract.test.ts src/medieval/terminal-controls.test.ts src/medieval/app.test.ts src/medieval/content-safety.test.ts --maxWorkers=1 --no-file-parallelism` passed 48 tests in 9 files (16.40s). The final `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 285 tests in 43 files (152.98s). `npx playwright test e2e/medieval-foundation.spec.ts --project=chromium` passed 2 Chromium tests (25.2s), including the visible materialized deck and zero-time unavailable movement. `npm run build` passed TypeScript, Vite (170 modules), and bundle-size checks. `git diff --check` passed before this roadmap update and is rerun afterward.

  Skips, warnings, and limitations: `npm run benchmark:medieval-foundation` was skipped because this bounded derived rendering slice does not materially change performance or persistence contracts. npm emitted the existing unsupported `email` configuration warning; Playwright also reported that `NO_COLOR` is ignored while `FORCE_COLOR` is set. No movement, collision, camera/focus, visibility, legend/help, interaction, cargo, repair, travel, or detailed renderer was implemented.
- [x] Implement grid movement, collision, camera/focus behavior, visibility rules if used, and inspectable seed state.

  Decision (2026-09-03): `jomon-navigation.ts` v1 derives legal cardinal/diagonal steps only from the validated deck-plan topology. `WorldDeckNavigationState` v1 stores the selected living courier and plan-derived coordinate in the sole full-envelope authority. Selection remains zero-time and assigns the tavern spawn; a successful move advances exactly one canonical action minute, appends/replays typed `deck-moved` evidence, and persists through the normal complete-envelope path. Hull, out-of-bounds, non-walkable, access, and diagonal-corner moves return bounded zero-time outcomes without state/history/storage mutation. Terminal-presentation v5 overlays the existing `@` marker over the unchanged 113 static cells, exposes source/time-labelled focus and creation provenance, and keeps a fixed full-deck camera. All deck cells are known; fog/visibility remains deliberately deferred.

  Compatibility and boundaries: `FoundationWorld` v14 / `MedievalWorldState` v12 add this bounded navigation state. Strict read-only v13-to-v14 conversion preserves immutable IDs/seeds/manifests, leaves an unselected courier coordinate absent, assigns only the canonical spawn to a valid selected courier, rebinds the affected replay checkpoint, and fails closed without overwriting corrupt/ambiguous data. IndexedDB remains layout v4; no new store, split record, migration of layout, manifest/replay authority, prototype save access, or public content surface was added. Props, tavern switching, cargo, repair, travel, quay departure, other actors, tactical play, legend/help, and the detailed renderer remain deferred.

  Verification (2026-09-03): focused medieval navigation/persistence/presentation tests passed after the final source changes; `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` passed 293 tests in 44 files (412.59s). `npx playwright test e2e/medieval-foundation.spec.ts --project=chromium` passed 2 Chromium tests. `npm run benchmark:medieval-foundation` completed its 9-fixture, 2-warm-up/9-sample offline Node matrix; all deterministic full-envelope ceilings remained within 512 KiB (largest after sequence: far-coast-deep, 313,541 bytes). `npm run build` passed TypeScript, Vite (171 modules), and bundle-size checks. `git diff --check` is rerun after this roadmap update.

  Warnings and limitations: npm emitted the existing unsupported `email` configuration warning; Playwright reported `NO_COLOR` ignored while `FORCE_COLOR` is set. Local explicit timeouts were raised for existing fixed deterministic replay/persistence fixtures whose full-envelope revalidation now exceeds the prior 5/20-second allowances; no global timeout or timing assertion changed. Benchmark timings are review-only, not CI or simulation inputs. No visibility, interaction, movement beyond this fixed local deck, or browser/platform storage performance measurement was added.
- [x] Add a player-visible map legend/help surface without replacing in-world readability.

  Decision (2026-09-03): terminal-presentation v6 adds a discardable, source-backed map-legend v1 for the currently visible closed glyph set: `@` active adult courier, `)` quay approach, `/` gangplank, `#` hull planking, and `=` open deck. Entries are canonically ordered from validated materialized terminal cells and their closed glyph-catalogue references; labels, palette tokens, paired non-colour cues, text/accessibility equivalents, content safety, evidence source, known world minute, and freshness remain derived rather than canvas-authored. The normal ASCII surface permanently shows the compact derived key in bounded rows, so help supplements rather than replaces in-world symbol readability. The existing remappable command-help control (default `?`) displays the fuller key, successful-local-step/blocked-collision time rule, current limitations, and the existing Escape cancellation behavior. Opening or closing help remains UI-only and zero-time. Current text is explicit that this is a known fixed local deck only: there is no cargo, NPC, hazard, travel, fog, or prop-action state.

  Detailed-renderer adapter v2 now carries a one-to-one copy of the legend, its entry metadata, and its accessible text under the equal-information parity contract; it still receives only terminal/sidebar/control projections and cannot receive a world, persistence, timer, or input-execution authority. The browser canvas continues to resolve visible characters only through the supplied terminal legend and closed catalogue, never through copied deck geometry or semantic definitions. A verified-world deck-plan seam removes the redundant full-envelope validation during terminal repaint after the terminal boundary has already validated it; the public map constructor remains fail-closed.

  Compatibility and boundaries: `FoundationWorld` remains v14, `MedievalWorldState` v12, navigation v1, terminal-controls v1, IndexedDB layout v4, and the complete persistence/replay envelope unchanged. This adds no world, manifest, migration, save, causal action, RNG, worker, cache, glyph, palette, input-authority, travel, fog, prop action, cargo, other-actor, or detailed-renderer implementation. It preserves the semantic palette roles and paired non-colour cues. [`README.md`](README.md), [`docs/jomon-deck-plan.md`](docs/jomon-deck-plan.md), and [`docs/performance-storage-baseline.md`](docs/performance-storage-baseline.md) document the surface, parity boundary, and the present-tense 113 static-cell plus source-backed courier-marker correction without rewriting dated measurements.

  Verification (2026-09-03): `npx vitest run src/medieval/ascii-glyphs.test.ts src/medieval/jomon-deck-plan.test.ts src/medieval/terminal-presentation.test.ts src/medieval/terminal-controls.test.ts src/medieval/detailed-renderer-adapter.test.ts src/medieval/renderer-browser-contract.test.ts src/medieval/app.test.ts --maxWorkers=1 --no-file-parallelism` passed 38 tests in 7 files (35.63s), covering canonical ordering, fail-closed legend validation, no hidden detail, zero-time help, remapping, accessibility, and adapter/browser-contract parity. `npx playwright test e2e/medieval-foundation.spec.ts --project=chromium` passed 2 tests (47.1s), including focused-canvas keyboard help, Escape dismissal, rebinding command help to `P`, and persisted zero-time confirmation. `npm run build` passed TypeScript, Vite (171 modules), and bundle-size checks. `npm run benchmark:medieval-foundation` passed its offline deterministic matrix; all nine complete envelopes remained within 512 KiB, with the largest after-due-events envelope at 313,541 bytes. `git diff --check` passed before this roadmap update and is rerun afterward.

  Required serial-suite warning: `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` did not pass: 284/294 tests passed in 39/44 files (643.10s), with 10 timeout-only failures. They were one each in delegation-social-hardening, performance-fixtures, persistent-person, and social-memory, plus six storage tests (configured 5/10/20-second limits). The focused legend set and Chromium browser run passed. Playwright emitted the existing `NO_COLOR`/`FORCE_COLOR` environment warning. No timeout budgets were changed. The benchmark is Node-only and does not measure browser responsiveness or IndexedDB timing.

  Acceptance: the player can read every current deck glyph in the normal ASCII surface or the remappable help overlay; the legend exposes no unrepresented world knowledge and does not claim cargo, NPC, hazard, travel, fog, or prop-action state.

#### 2.2 Crew continuity

- [-] Generate the initial household roster, roles, personal equipment, relationships, histories, eligibility, and active-crew representation wholly from the world seed and resolved configuration; define its reproducibility and validation contract.

  Initial-household contract implementation and verification update (2026-09-04): `src/medieval/initial-household.ts` v1 is the sole immutable zero-time household owner. It retains the existing `crew:<index>` seed stream and generated output while owning the six-member roster, canonical order/IDs, distinct names, unique closed roles, conversations 1–5, role-owned equipment/history, current `eligible: true` policy, and all directional relationship records. Reciprocal directional link presence is mandatory, but reciprocal standing and basis are intentionally not forced equal. Generated player-facing identity/history material is audited by the existing content-safety policy.

  `FoundationCrewMember[]` remains the only immutable roster representation. Its selection-ready active-crew view is a discardable canonical eligible-member projection containing only stable ID, name, role, and conversation. It has no selected courier, persistent-person/world/map/frontier/browser state, initial-world person seed, or hidden commitment; it is never persisted as a duplicate roster. It does not advance time, append causal history, schedule simulation, mutate state, change navigation, or write storage. The mutable selected courier remains `state.courier.initialCourierId` under the later selection/switch/succession owner. There are no individual rerolls: only a changed normalized seed or resolved configuration changes the household.

  Creation, manifest recreation, FoundationWorld, mutable-world state, persistent-person, causal replay, and IndexedDB validation reproduce and fail closed on malformed, unsafe/unclassified, or non-reproducible household data without mutating supplied records. Persistent people remain deterministic expansions of the validated immutable roster; initial-world person seeds and frontier commitments remain non-instantiated and cannot enter the household or active-crew view. [`docs/initial-household-contract.md`](docs/initial-household-contract.md) records ownership and boundaries. FoundationWorld remains v14, MedievalWorldState v12, WorldDeckNavigationState v1, manifest/replay contracts, and IndexedDB layout v4; no generator/RNG stream, save envelope, migration, browser behavior, or selected-courier authority changed.

  Verification (2026-09-04): `npx vitest run src/medieval/initial-household.test.ts src/medieval/world-manifest.test.ts --maxWorkers=1 --no-file-parallelism` passed 12 tests in 2 files. The focused storage forgery test passed 1 test (39 skipped). The required `npx vitest run src/medieval/*.test.ts --maxWorkers=1 --no-file-parallelism` did not pass: 280/306 tests passed in 34/45 files (969.90s), with 26 timeout-only failures in delegation-social-hardening (1), delegation (3), foundation-hardening (4), generation-properties (1), jomon-navigation (1), management-sidebar (1), performance-fixtures (2), persistence-layout (2), persistent-person (1), social-memory (2), and storage (8). No timeout budget changed; there were no assertion or type failures. `npm run build` passed (TypeScript, Vite transformed 172 modules, and bundle-size check); `git diff --check` passed. Playwright was skipped because no browser-visible behavior changed. `npm run benchmark:medieval-foundation` was skipped because generated/persisted output and performance behavior remain unchanged. The failed required serial verification leaves this task active; do not activate the initial-courier-selection task yet.

- [ ] Implement initial-courier selection in world creation. It presents the deterministic eligible roster, changes no world time, permits no individual rerolls, and fixes the selected courier as active until a later tavern-based switch or succession.
- [ ] Implement tavern-based voluntary switching through an operated physical prop.
- [ ] Implement permanent courier death/departure and deterministic transfer of the active perspective to an eligible surviving courier.
- [ ] Display crew availability and loss consequences in a physical vessel surface.

Acceptance: the same world manifest recreates the same valid initial household; choose an initial courier without rerolling; switch active crew in the tavern; lose an active courier; continue as the same eligible successor after reload; observe the lasting household consequence; finalise a crewless household as a read-only chronicle.

#### 2.3 Physical vessel interactions

- [ ] Implement proximity/operation rules for every Phase 2 vessel prop.
- [ ] Add compact contextual key-choice prompts, cancellation, keyboard remapping compatibility, and accessible text labels.
- [ ] Make the chart table, hold, repair space, stores, berths, galley, tavern, and gangplank each expose a distinct minimal action or readout.
- [ ] Persist prop state and show action feedback in the message/status surfaces.

Acceptance: every named space has a represented prop, an operation affordance, an accessible label, and a tested outcome.

#### 2.4 Quay-to-vessel browser proof

- [ ] Start at a quay; board by gangplank; walk to the tavern; select another crew member; operate a vessel station; return to and leave through the gangplank.
- [ ] Add actual-browser coverage for the full path and focused headless determinism coverage.
- [ ] Verify the flow in ASCII mode and ensure its world-state/output contract is consumable by the later detailed renderer.

Acceptance: the browser test completes this exact path through real user input with no abstract hub or terminal shortcut.

### 3. Physical trade and settlement economy

- [ ] Define a small, named commodity set with source, use, weight/bulk, condition, handling requirement, failure mode, and buyer for every entry.
- [ ] Add cargo capacity, loading, unloading, spoilage/damage/loss, recovery, and vessel-hold interaction.
- [ ] Build one named settlement profile with water relationship, labour, authority, services, demand, local pressure, and visible civilian purpose.
- [ ] Add a physical settlement trading location and one contract that creates a material burden, refusal outcome, delivery outcome, and later visible consequence.
- [ ] Model price/stock/demand changes as seeded persistent local state—not a global money-only shop.

Acceptance: acquire cargo at one physical location, transport it, deliver or fail it at another, and observe a durable market or relationship change on return.

### 4. Waterway routes and settlement network

- [ ] Define the first connected river/canal/estuary/coastal settlement network with named route profiles.
- [ ] Define how routes, travellers, charts, rumours, cargo marks, and institutions reveal frontier regions before Jomon reaches them; travel can then trigger deterministic generation and persistent exploration of those regions.
- [ ] Add seeded route knowledge, river condition, weather/season constraint, toll/access condition, and competing traffic where relevant.
- [ ] Make route comparison a chart-table action and departure/arrival a gangplank/quay action.
- [ ] Implement active-play-only route time and an inspectable route-reckoning readout.
- [ ] Make a route outcome change cargo, access, conditions, or local history in a later decision.

Acceptance: choose a route for a visible reason, travel only while actively playing, arrive through a quay, and see a persistent consequence on a revisited route or settlement.

### 5. Persistent world partitions and revisitation

- [ ] Give the vessel, quays, settlements, approaches, roads, waterways, and expedition sites stable world-space identities.
- [ ] Apply the foundation simulation-fidelity tiers to partition streaming, including deterministic catch-up and replay of distant settlement summaries.
- [ ] Persist crew, cargo, altered props, caches, actors, weather effects, route consequences, time, and camera direction within the new medieval schema.
- [ ] Stream only distant partitions; specify and test the resident partition budget.
- [ ] Define corruption recovery, incompatible-save behavior, and schema evolution before each persistence change.

Acceptance: alter a place, leave it, reload, return through normal play, and find the same consequential state while an unseen settlement and recurring agent have advanced deterministically without loading unrelated partitions.

### 6. Equal-depth hazards, conflicts, and regional ecology

- [ ] Apply the social-memory model to human-conflict content so the same people can recur as allies, rivals, witnesses, employers, or consequences without scripted plot progression.
- [ ] Exercise the active-play simulation scheduler through changing named people, settlements, markets, and routes.
- [ ] Populate the foundation history surfaces at Jomon and settlements with witness accounts, ledgers, rumours, notices, cargo evidence, and changed work sites.
- [ ] Implement one complete human-conflict contract: setup, readable intent, choice, resolution, persistent mutation, and later follow-up.
- [ ] Implement one complete wilderness contract using weather, terrain, exposure, animal behavior, disease, or a specifically sourced mystical phenomenon with readable counterplay.
- [ ] Implement one complete operational-hazard contract involving vessels, cargo, works, or infrastructure.
- [ ] Establish common telegraph, consequence, and follow-up interfaces so all three danger families have comparable depth.
- [ ] Add seasonal and local ecology state that changes trade, settlement life, routes, or tactics.

Acceptance: each danger family has a distinct playable scenario with readable counterplay and a visible later-world effect; at least one named person reacts to a remembered outcome through the original social-memory model.

### 7. Medieval tactical overhaul and grounded guardians

- [ ] Define a dense but fast tactical vocabulary: movement, exertion/stamina, commitment, guard, parry, posture, targeted components, retreat, recovery, health, injury, and seeded chance.
- [ ] Implement renderer-independent intent data, target previews, and positional responses for both presentation modes.
- [ ] Implement bounded, legible power combinations through equipment, relics, learned techniques, temporary preparations, crew support, enemy weaknesses, and environmental interactions. Avoid numerical-only stacking and keep resolution fast.
- [ ] Replace prototype content in the new path with original people, animals, machinery, wrecks, siege works, fortified positions, industrial hazards, and rare sourced mystical threats; do not add a generic mage class.
- [ ] Create procedural multi-cell grounded or mystical guardians from original body, component, attack, terrain, reward, and aftermath modules.
- [ ] Test tactical determinism, readability, combat-resolution speed, defeat/escape consequences, and interaction with crew/cargo/world state.

Acceptance: a player can read a telegraphed tactical problem, choose a grounded response, and carry its material consequence back to Jomon or a settlement.

### 8. Content families and procedural composition

- [ ] Create reusable authored families for distinct regions, waterways, settlements, workshops, markets, hazards, wildlife, human groups, enemies, tools, cargo, contracts, crew roles, relics, and guardians.
- [ ] Create vessel-expansion families whose upgrades visibly alter Jomon’s deck, capabilities, upkeep, crew work, cargo decisions, or route options.
- [ ] Create era-aware content variants and generative combinations so base, NG+, and NG++ create new pressures and possibilities from the same grounded systems without content exhaustion or numerical-only scaling.
- [ ] Require generated places to declare material purpose, water/terrain/season relationship, local pressures, rewards, services, and downstream world links.
- [ ] Add seeded fixtures, validity checks, encounter-readability checks, and reward-distribution checks for each family.
- [ ] Grow content only after the family has a player-visible purpose in the vessel/trade/history simulation.

Acceptance: generation sweeps produce valid, distinct locations whose content influences a real later choice.

### 9. Coverage, playtesting, and art

- [ ] Cover every player-visible vessel, crew, trade, route, persistence, tactical, and death/replacement capability with focused tests and actual browser input.
- [ ] Recreate the playtest protocol only after the relevant medieval player surfaces and content families are stable.
- [ ] Produce original Rogue-inspired visual assets only after the map grammar, glyph taxonomy, and renderer requirements are stable.
- [ ] Implement the optional detailed renderer from the Phase 1 adapter contract only after the ASCII game and its systems are stable; verify it conveys identical consequential information and meets accessibility requirements.
- [ ] Run full verification and record exact pass/fail/skip status for each completed slice.

Acceptance: browser playtests and automation cover the complete core loop without prototype terminology or presentation dependencies.

## Deferred tuning decisions

The direction above is settled. These implementation values are intentionally delegated to system design and must be recorded here before the corresponding system is marked complete:

- `WorldGenerationConfig` preset names, defaults, ranges, validation/rejection rules, and user-facing descriptions for every advanced setting.
- The initial world size, initial population, frontier-generation cadence, and storage/performance budgets that fit the 8 GB desktop-browser baseline.
- The management sidebar’s exact information density, tab/layout rules, source/freshness labels, and the precise split of information among it, map marks, physical notices, messages, ledgers, and conversations.
- Delegated-task risk policy: which tasks are safe, hazardous, or require an explicit player confirmation; what warnings protect a person or Jomon from a terminal outcome.
- The base/NG+/NG++ thresholds and signatures: how active-play time and Jomon growth are weighted, which world pressures shift, and how the NG++ plateau remixes without raw numerical inflation.
- The first mystical-effect families, their rarity bands, provenance, counterplay, and visual language; especially the ultra-rare rule for an effect that can save Jomon.

## Delivery and verification rules

- Work in one cohesive, maintainable system unit at a time. Keep exactly one roadmap item marked `[-]`; build foundations and their tests before expanding player-facing content.
- Before a high-risk save, renderer, world-streaming, or combat change, add a decision-complete implementation plan beneath the relevant item or in a narrowly scoped linked design document.
- Prefer documented domain types, small modules with explicit ownership, deterministic/pure simulation functions, bounded state, and narrow public APIs. Do not add a content exception that bypasses a foundation contract.
- Keep the code navigable for humans and AI agents: name data by its game meaning, state invariants beside their types, provide seeded fixtures, and update this plan when an architecture decision changes.
- Preserve unrelated changes. Do not clean, migrate, or reuse old saves outside the explicit clean-break implementation.
- Each implementation task needs focused tests first. Completed slices also run, when available: `npm test`, `npm run test:autoplay:tasks`, `npm run test:e2e`, `npm run build`, and `git diff --check`.
- Browser coverage must execute the feature through actual UI input; task-ID mapping is not sufficient.
- Record failed, skipped, unavailable, and incomplete verification plainly. A task is not complete merely because it compiles, has a test file, or resembles prototype capability.
- Keep consequential randomness seeded and expose enough state to reproduce a reported play session.

## Final legacy prototype retirement `[ ]`

This is intentionally the final repository task. Do not start it while the isolated prototype is still useful for implementation comparison or regression diagnosis. Start only after every applicable medieval phase through Phase 9 is complete, the fully medieval browser build is verified, and no open task names a prototype module as a temporary diagnostic dependency.

- [ ] Perform a final import, route, build-output, dependency, asset, script, and test audit. Identify the exact legacy files that are unused, unreachable from the completed medieval application, or no longer useful as diagnostics; keep only neutral tooling/configuration and any files still demonstrably required by the finished medieval build.
- [ ] Remove the superseded space-fiction application and all now-irrelevant implementation, tests, browser routes, persistence migrations, assets, fonts, audio, generated sprite artefacts, autoplay/telemetry/campaign tooling, and package scripts. Remove `?prototype` and its browser coverage once its diagnostic purpose has ended.
- [ ] Simplify the repository's public commands, documentation, ignore rules, and build configuration so they describe and execute only Jomon's completed medieval product and its current maintenance tooling.
- [ ] Re-run the complete medieval verification suite, inspect the production output to confirm it contains no legacy chunks or assets, confirm no old save namespace is opened, and record every retained compatibility/tooling file with its reason.

Acceptance: a clean checkout contains only the completed medieval game and neutral supporting tooling; the normal build, tests, documentation, browser routes, assets, and package commands contain no unreachable or obsolete prototype product code. This task is complete only after the removal audit and full verification are recorded here.
