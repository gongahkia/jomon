# Legacy `play` Command

The canonical gameplay surface is [Browser Table](../browser-table.md). It runs in `web/` and is
published as a static browser application; Python does not own game state or policy decisions.

`kenjaku play` remains a compatibility artifact exporter while existing research-run workflows are
migrated. It is not the player-facing surface.
