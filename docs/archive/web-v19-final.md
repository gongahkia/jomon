# Web v19 archive

The final browser implementation is preserved in local Git at commit
`de1c1e86c3ce6e962ddd547f57830c636052b870`.

- branch: `archive/web-v19`
- annotated tag: `jomon-web-v19-final`

The requested expected starting commit was `614d6fd`, but the clean repository
actually started this pivot at `de1c1e8`. The archival references therefore
point to the verified pre-pivot `HEAD`.

The active tree is being replaced by a small Python 3.11 `curses` game. Git is
the full source archive; browser-era contracts and reports retained under this
directory are historical context, not active implementation requirements.
