{
  "name": "golf-with-your-enemies",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "server": "tsx server/index.ts",
    "dev:online": "npm run server",
    "build": "tsc --noEmit && vite build",
    "check:server": "tsc --noEmit -p server/tsconfig.json",
    "test": "npm run test:core-foundation && npm run test:core-turns && npm run test:other",
    "test:core-foundation": "vitest run tests/core.test.ts -t 'course generation|clubhouse economy|public voting'",
    "test:core-turns": "vitest run tests/core.test.ts -t 'turns, shared rules, and bots'",
    "test:other": "vitest run tests/app-controller.test.ts tests/feedback.test.ts tests/hazards.test.ts tests/markup.test.ts tests/physics.test.ts tests/preferences.test.ts tests/render.test.ts",
    "check": "npm run build && npm run check:server && npm run test",
    "capture:levels": "node scripts/capture-levels.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "@types/ws": "^8.18.1",
    "tsx": "^4.23.12",
    "typescript": "^5.8.3",
    "vite": "^6.3.3",
    "vitest": "^3.1.2"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
